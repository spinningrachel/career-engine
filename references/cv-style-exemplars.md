# CV Style Exemplars — {{USER_FULL_NAME}}

Before/after pairs from CVs the user hand-edited before sending. The "after" versions are the
governing register for skills sections and bullet compression — they outrank any default
example in the plugin's doctrine (`skills/writer-craft/cv.md` §6b).

This file is optional. When it exists in the user's `career-data` (`${CAREER_DATA}/references/cv-style-exemplars.md`),
the cv-writer loads it at every spawn and treats each "after" as the standard the draft must
match. When it does not exist, the cv-writer follows §6b's generic examples alone.

How to grow this file: whenever the user hand-edits a delivered CV before sending it, capture
the delivered version and the user's final as a new exemplar pair below.

---

## Exemplar 1 — {{SHORT_LABEL, e.g. "Skills section, <role type> application, <month year>"}}

**Delivered (before):**

```
{{DELIVERED_TEXT — paste the pipeline's delivered version of the section, verbatim}}
```

**User's final (after):**

```
{{FINAL_TEXT — paste the user's hand-edited final version, verbatim}}
```

**What the edit teaches (optional):** {{ONE_OR_TWO_LINES — the pattern the writer should generalize, e.g. "items were restating the heading's category word; final drops the repeated word from every item"}}
