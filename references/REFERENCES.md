# Reference Files Index

All reference files live in this directory. Agents load files using the absolute path to this folder.

**Maintenance:** user-shared materials are folded into these files via the `update-refs` skill ("update my references") — classification against this index, approval-gated writes, protected approved content. A new reference file may only be created after the usage interview in that skill, and must get a row in this index plus loading-table entries in every consuming agent before the add counts as complete.

| File | What it contains | Load when | Do NOT use for |
|---|---|---|---|
| `01-writing-rules.md` | **Configuration and rules.** Section 1: priority framework, attribution rules, fabrication guards, framing rules, JD term guardrails — read first. Section 2: operational identity (targets, differentiators, voice). Section 5: tone calibration and proof points. Section 8: contact details, education, tools, skills. **Setup required** — fill in all `{{...}}` placeholders before first run. | Every task — load first. Rules and constraints govern all other agents. | Approved bullets, summaries, testimonials, portfolio — those are in `02-professional-background.md`. |
| `02-professional-background.md` | **Router to career content bank + Motivation Bank sub-files.** Load this file first, then follow its routing table to the `background/` sub-files: `background-motivation-bank.md` — the Motivation Bank (`\| Tags \| Motivation \|` table, letter-writer's **primary** content/voice source, append-only); `background-cv-summaries.md` — approved CV summaries; `background-approved-bullets.md` — approved CV bullets; `background-role-facts-*.md` — per-company role facts (every claim must be grounded here); `background-testimonials.md`; `background-portfolio.md`; `background-cross-cutting-skills.md`. **Setup required** — fill in role facts before first run. | Every CV or cover letter writing task (role facts, bullets, summaries); every letter-writer run (Motivation Bank). | Rules and operational constraints — those are in `01-writing-rules.md`. |
| `03-framework.md` | **Positioning, voice, methodology, and domain narratives — the primary source of truth about the user.** Agents form their understanding of the user from this file first. Category frame, core positioning, value pillars, proof points, ICP, messaging, taglines, differentiators, career-shift posture, domain depth — all inline. **Professional methodology and POV routes to `framework/framework-*.md` sub-files** (PLG, GTM, research/docs, growth/B2C, leadership/AI, experience/POV) — load the relevant sub-file when the role's emphasis maps to a specific methodology. | Cover letter writing, career coaching, strategic framing, LinkedIn recommendations (background guidance). | Rules, approved bullets, or raw facts. |
| `linkedin-profile.md` | **Snapshot of the user's current LinkedIn profile** (from their LinkedIn PDF export). The canonical base for every LinkedIn recommendation — orchestrator Step 8 and all linkedin-coach modes analyse against this content. Replaced wholesale via update-refs when the user exports a fresh PDF. **Optional** — when missing or still templated, LinkedIn outputs run in fallback mode (raw signals, no profile analysis). | Orchestrator Step 8 (LinkedIn updates file); every linkedin-coach mode that reads the profile. | The user's goals or positioning — that is `03-framework.md`. LinkedIn is a tool being improved, not a source of truth about the user. |
| `cv-self-check.md` | Mandatory pre-submission self-check for CV output. Covers ATS, summary, key achievements, experience, earlier line, header, and body word count. | Every CV draft and revision — run before returning any output. | Cover letters. |
| `pipeline-preferences.json` | **Per-install config plus the single source of truth for job-search preferences.** `output_folder`, and `database_id` (when a database backend is configured) are the only required keys. There is no `cv_template` or `word_templates_path` key (2026-07-04 fix, R-38) — CV/cover-letter/Hebrew templates resolve by fixed filename from `career-data/references/templates/`, never a config lookup. Every other key is optional: `database_backend`/view-URL fast-paths, `gap_handling`, `location_compatibility`, `favorite_brands`, `preferred_job_sites`/`local_job_sites`, `target_titles`, `title_variants`, `remote_preference`, `exclusion_patterns`, `default_search_time_range`, `seniority_floor`, `target_function`, `industry_fit`, `company_stage_fit`, `employment_type_preference`, `coaching_prioritization`, and `screening_answers`. | Before any sourcing, scoring, or coaching step, and by every pipeline for its required config keys. | Content writing or document production. |
| `discipline-emphasis-signals.md` | **Discipline → emphasis-signal map for the coach's `Role emphasis` read (2026-07-23).** Per common discipline (marketing, sales, product, engineering, data, CS, HR, finance, design): the signals that reveal what a JD actually weights, the "same term means different work at different companies" disambiguation rows, and the cross-discipline reading rules (placement beats presence, repetition reveals fear, verb strength ranks, omissions signal the operating model). Generic — no user-specific content. | `career-coach` (before writing any `Role emphasis`, Options 1 and 2). | Personal capability data — `Role emphasis` names the role's motion in generic discipline terms only; the writers map it to the user from `02`/`03` in career-data. |
| `cv-template-default.dotx`, `cover-letter-template.dotx` | **New-user defaults only** — copied into a new user's `career-data/references/templates/` (as `cv.dotx` and `cover-letter-template.dotx`) at setup. The plugin never reads these itself at runtime — every real export resolves `$CV_TEMPLATE`/`$CL_TEMPLATE` from `${CAREER_DATA}/references/templates/` by fixed filename, no config key (2026-07-04 fix — `cv_template` and `word_templates_path` are retired). | `skills/career-engine-setup/SKILL.md`, at first-time career-data scaffolding only. | Live pandoc DOCX production — that always reads from career-data, never from the plugin. |
| `cover-letter-templates-default.md` | **New-user default for `cover_letter_templates.md`** — a generic, parameterized Template A (Cold/Scaffold) / Template B (Warm/Woven) structure doc with placeholder tokens, no personal data. Copied into a new user's `career-data/references/templates/cover_letter_templates.md` at setup; the letter-writer selects between the two templates per JD/context (`agents/letter-writer.md` Step 0.7) and personalizes it over time as delivered letters accumulate. | `skills/career-engine-setup/SKILL.md`, at first-time career-data scaffolding only. | Any user's actual personalized template content — that lives only in `career-data`, never here. |
| `shared-voice-rules.md` | **Canonical cross-surface voice layer — for LinkedIn and freelance surfaces.** §1: punctuation prohibitions (em dashes, context-specific colons). §2: banned vocabulary (AI patterns, hollow filler, surface-specific bans). §3: banned phrases and constructions (named anti-patterns, opening/transition phrase bans, false dichotomies, oppositional rhetoric, vague phrases). §4: structural anti-patterns (antithesis/pivot, false range, approach-announcement, contrived tricolons, -ing appendages). §5: sentence mechanics (passive voice, subject-first, length variation, synonym cycling, filler phrases). §6: idiom prohibition. §7: positive writing standards. §8: LinkedIn post structure framework (engagement philosophy, Formats A/B/C, development process, quality checklist). Rules marked [CV], [CL], [LI] are surface-specific; unmarked rules apply everywhere. **As of the writer-craft consolidation, `cv-writer`, `letter-writer`, and the humanizer no longer load this file** — its §1-7 content (CV/cover-letter-relevant subset, trimmed) now lives inline in `skills/writer-craft/SKILL.md`, and the humanizer-relevant portion now lives in `skills/humanizer/SKILL.md`. This file remains canonical and unmodified for its LinkedIn and freelance consumers. | Loaded by: `linkedin-post-writer`, `linkedin-post-reviewer`, `freelance-manager`, and the `linkedin-coach` standalone skill — alongside the surface's own skill. | Personal data — that is `career-data`. Procedural steps — those are in agent files. CV, cover-letter, or humanizer doctrine — that is `skills/writer-craft/SKILL.md`. |
| `cv-style-exemplars.md` | **Blank template** for the user's before/after CV edit pairs — the delivered version vs. the user's hand-edited final. Copied to `career-data/references/` and filled per exemplar; the filled copy is the cv-writer's governing register for skills-section and bullet compression style (`skills/writer-craft/cv.md` §6b). Optional — cv-writer loads it only when the filled copy exists. | Loaded by: `cv-writer` (conditional, from `${CAREER_DATA}`). The plugin copy is a blank template only. | The user's real exemplar content — that lives in `career-data`. |
| `cover-letter-self-check.md` | **RETIRED** — folded into `skills/writer-craft/letter.md` §11 (Cover Letter Self-Check; sub-file since the 2026-07-22 split) during the writer-craft consolidation. | N/A | N/A |
| `humanizer-target-metrics.md` | **RETIRED** — folded into `skills/writer-craft/SKILL.md` §12, later relocated to `skills/humanizer/SKILL.md` (see writer-craft frontmatter) during the writer-craft consolidation, then relocated again to `skills/humanizer/SKILL.md` (Humanizer Mechanics, Quantitative Final Gate) when the humanizer got its own dedicated skill. | N/A | N/A |
| `voice-calibration-method.md` | **Generic, reusable voice-calibration methodology, two formats** — not personal data, no user-specific content. The procedure for producing a durable voice calibration file (e.g. `${CAREER_DATA}/references/voice-calibration-coverletters.md`) from a corpus of delivered writing. **Templates-aware format** (2026-07-04): a short Role & Intent / Structural Execution / Syntax & Generation Guardrails brief, used when the surface has a structure-templates doc (e.g. cover letters' `templates/cover_letter_templates.md`) — it sets voice on top of a structure the templates doc already owns, never duplicating its dials or invariants. **Six-dimension format** (fallback, when no structure-templates doc exists for the surface, or the archive is empty/unreachable): the original sentence-length/word-choice/openers/punctuation/transitions/tics breakdown, falling back to `03-framework.md` §Voice and tone when needed. Not a live pipeline step — run manually (by a human or a future agent) whenever a fresh calibration is needed: initial setup, new delivered letters added, or a new output type needing its own calibration file. Replaces the retired `voice-analyst` pipeline agent, which ran this same analysis per-run; the analysis now produces a durable file instead. | `career-engine-setup` (offered after cover letters are stored, Phase 3); any time a calibration file needs to be generated or refreshed. | Personal data — the output file it describes how to produce goes in `career-data`, never in this file. Procedural pipeline steps — this is a standalone methodology doc, not a pipeline component. |
| `career-data-skill-handoff.md` | **Canonical create / install / update procedure for the `career-data` skill (= "Appendix A").** Encodes the platform reality (plugin runs in Cowork/Code, not Chat; skills are shared between Chat and Cowork; `/skill-creator` lives in Chat) and the Cowork → Chat `/skill-creator` handoff with the four-step visual the setup agent must show. Also covers the Code direct-write path and points to the update-prompt format for ongoing edits. Referred to elsewhere as "the Appendix-A handoff." | Setup skill (build-the-skill step), orchestrator and update-refs (Cowork update staging), any place that emits a `career-data` create/install/update handoff. | Personal data — never embed real values in this file; the agent fills the template at runtime outside the repo. |
| `career-data-update-prompt-format.md` | **Canonical template for `career-data` *update* prompts** (edits to an already-installed skill). Fixed context block + "The fix" + verification. Used by pipeline agents and the career coach to generate ready-to-paste Chat update prompts. Sibling to `career-data-skill-handoff.md` (which covers first-time creation). | Any agent generating a career-data update prompt (new-application Step 7f, career coach Option 6, update-refs, orchestrator motivation-bank promotion). | First-time skill creation — that is `career-data-skill-handoff.md`. |
| `career-data-structure.md` | **Structure contract for the `career-data` skill** — the canonical list of load-bearing files and fixed section headings, plus the Section 5 Motivation Bank table-format spec. Distinguishes safe content edits (within sections) from structural changes (rename/renumber/reorder/delete a heading or file, or change the Bank table format) that must go through `update-refs`. Shipped into the user's skill at creation as `references/_STRUCTURE-DO-NOT-CHANGE.md` so the contract sits in front of Chat on every future edit. | Creating or updating `career-data` (setup handoff, `update-refs`); any time a section/file rename, renumber, or reorder is in question. | Personal data, procedural steps, or writing craft. |
