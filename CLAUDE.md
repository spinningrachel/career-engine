# CLAUDE.md — Career Engine Plugin

Working instructions for Claude when editing, extending, or maintaining this plugin.

---

## ⛔ MANDATORY STOP — READ BEFORE DOING ANYTHING ELSE

**You are not done with any plugin edit session until the QA agent has run and passed. No exceptions.**

This means: after completing any set of changes — no matter how small — you MUST invoke the QA agent (`agents/qa-plugin.md`) before telling the user the work is complete. This is not optional and cannot be skipped because the changes "seem clean" or because you "already checked manually." Manual checking is how drift accumulates silently.

**The plugin is a single build.** There is no second personalized copy to keep in sync: the user's personal data lives entirely in the external `career-data` skill (see *Data layer* below), which the plugin never contains. QA validates the one shipped artifact and confirms it holds zero personal data.

**How to invoke:** Spawn the QA agent by reading `agents/qa-plugin.md` and following its instructions. Pass it the repo path and the built `.plugin`.

This gate applies to: any content edit, any rename, any new file, any property name change, any structural change, any cross-version sync. If you edited even one file, run QA.

## ⛔ NO SILENT CAPABILITY REMOVAL — every removal needs a quoted user instruction

**Removing, disabling, or substituting-away any capability the user relies on — an input an agent reads (a file, an archive, a database property), an output an agent produces, a pipeline step, a quality pass, or a read of the user's own material — requires the user's explicit instruction, quoted in the commit message and the changelog entry.** "This new mechanism makes the old read unnecessary," "this is now redundant," "the consolidated file covers it" are NOT authorization — they are exactly how the incident below happened. Additions and tightenings can ride on an agent's judgment; **removals never can.** If a change you're making seems to imply a removal, stop and ask — the user deciding "yes, remove it" costs one question; a silent removal cost weeks of degraded output.

**⛔ Named anti-pattern: the June-30 archive-read removal.** On 2026-06-30 (commit 060f3b2, nominally a coach upgrade), the letter pipeline's read of the user's real delivered letters was quietly replaced with "Read `$PIPE/voice-calibration.md`. Your calibration is complete — no archive read needed." The user never asked for the removal and never knew it happened. Every pipeline letter from that day forward was written by agents that had never seen a single sentence the user actually wrote — a primary driver of a multi-week cover-letter quality collapse, discovered only when the user reported letters "consistently coming out terrible" on 2026-07-16. The same session also found the Annotated Exemplar silently dropped in the 2026-07-02 consolidation. Both were restorations the user had to order after the fact; neither removal was ever hers to begin with.

**QA enforcement:** the QA agent's diff-scoped sweep (Phase 0-D) checks every removal-shaped hunk in the diff — a deleted read instruction, a deleted output, a step deletion, an "instead of"/"no longer needed"/"X replaces Y" edit — for a quoted user instruction in the commit message or changelog. A removal without one is a FAIL.

---

## Data layer: the `career-data` skill

**Single-build architecture** — the plugin ships as **one build**: the public repo. It contains only code (agents, skills) and **blank reference templates** carrying `{{...}}` placeholders — no personal data, no second version, nothing to keep in sync. The user's personal data lives entirely in the external `career-data` skill described below.

The user's personal data — filled `01/02/03`, delivered letters, LinkedIn snapshot, job preferences, pipeline preferences, personal `.dotx` — lives in a separate, user-installed skill named **`career-data`**, outside the plugin. Plugin upgrades never touch it. Agents resolve `career-data` at run start and read the user's real data from it; the plugin's blank templates are the new-user fallback only.

**Placeholder resolution.** The plugin's instruction files keep `{{...}}` placeholders literally — they are NOT substituted at install, because substituting them would personalize the shared build. Agents resolve them at runtime from `career-data`: identity values (`{{USER_FIRST_NAME}}`, `{{USER_FULL_NAME}}`, `{{USER_LAST_NAME}}`, `{{USER_PROFESSION}}`, `{{USER_FUNCTION_SENIORITY_HIERARCHY}}`, city/country, etc.) from `career-data` `01-writing-rules.md` §8; all per-install config from the `career-data` config (`${CAREER_DATA}/references/pipeline-preferences.json`): `database_backend`, `database_id`, `output_folder`, `draft_dir_url_base`, `database_new_view_url`, `database_edit_view_url`, `database_interested_view_url`, `database_hold_view_url`, `database_researched_view_url`, `database_cv_ready_view_url` (all six view URL keys are optional fast-paths; when populated, pipelines skip the 59KB DB discovery fetch), `gap_handling`, `cv_footer.inject` (boolean, default `true` — whether the pipeline appends `static-cv-footer.md`'s Education/Languages content to every CV; set `false` if the user manages that content herself outside the pipeline, e.g. her own Word macro run after export — see the cross-file-contract row below). Setup writes them all; the orchestrator and the standalone entry skills read them at run start and resolve every `{{CONFIG}}` placeholder from them, stopping ONLY if a **required** key (`output_folder`, or — when a database backend is configured — `database_id`) is missing (R-38). **Every other key is optional: a run completes without it and a config-health notice lists what is empty/missing, so an older config never breaks a pipeline.** **Backward compatibility:** legacy names `notion_database_id` (→ `database_id`), `notion_needs_editing_view_url` (→ `database_edit_view_url`), and `location_compatibility.notion_property` (→ `database_property`) are still read; prefer the `database_*` names. (The internal `$NOTION_DATABASE_ID`/`$NOTION_NEEDS_EDITING_VIEW_URL` shell vars are the Notion adapter's names and are unchanged — see the *Database backend* glossary entry.) (The literal `{{PLACEHOLDER}}` template syntax in `linkedin-coach`/`personal-brand` agent-output instructions is a different thing and is unaffected — it stays literal in both contexts.)

**Template resolution (R-38, 2026-07-04 fix) — no config key, ever.** `cv_template` and `word_templates_path` are retired config keys — CV/cover-letter/Hebrew templates are never configured, they resolve by fixed filename convention inside career-data: `${CAREER_DATA}/references/templates/cv.dotx`, `cover-letter-template.dotx`, `cvHe.dotm` (note: `.dotm`, not `.dotx`), `he-letter.dotx`. The plugin never reads a template from an external OS path or falls back to its own `references/` default at runtime — the plugin's own default `.dotx` files exist only to be copied into a new user's `career-data/references/templates/` at setup (see `career-engine-setup/SKILL.md`). **Confirmed production bug this replaces:** the cover-letter DOCX conversion script hardcoded the plugin's own default template for every export, silently ignoring the user's actual personalized cover-letter template on every run. **Same fix applied to the CV footer (2026-07-09) — `$CV_FOOTER` = `${CAREER_DATA}/references/static-cv-footer.md`, `$CV_FOOTER_HE` = `${CAREER_DATA}/references/static-cv-footer-he.md`.** These are markdown, not `.dotx`, so they live at `references/` directly, not `references/templates/`. **Confirmed production bug, not theoretical:** `convert-cv.sh` hardcoded the plugin's own `skills/career-engine-export/static-cv-footer.md` for every CV export; that plugin-shipped file had accumulated one real user's actual degree/university content, so every installation of this plugin was silently appending someone else's real Education/Languages section onto every user's exported CV. The plugin's own copies are now blank `{{...}}`-placeholder defaults only, copied to career-data and filled in with the user's real answers at setup (`career-engine-setup/SKILL.md`, Document templates).

### Skill ownership map — who reads what, from where

The Desktop app has three runtime environments. They do **not** share a skill store:

| Environment | Where it reads skills from | Notes |
|---|---|---|
| **Chat** | Desktop app skill store | Skills installed via Customize → Skills |
| **Cowork** | Desktop app skill store | Same store as Chat — same install |
| **Code (CLI)** | `~/.claude/skills/` on local disk | Separate location; synced one-way from Desktop app |

#### The plugin (`career-engine`)

Installed as a `.plugin` file via **Customize → Connectors → Personal plugins**. Available in all three environments after install. Contains only code — agents, skills, blank templates. No personal data.

#### The `career-data` skill

Installed as a `.skill` file via **Customize → Skills**. The Desktop app install is canonical — it serves Chat and Cowork. When the Desktop app installs a skill, it also writes a copy to `~/.claude/skills/` so Claude Code can read it.

#### Drift prevention (Desktop app users)

With a single plugin build there is no cross-version drift. The remaining drift risk is between **copies of `career-data`**: the canonical Desktop app install vs. a stale copy in `~/.claude/skills/`. This only applies to users running both the Desktop app and Claude Code CLI.

**One-way sync: Desktop app → Code.** The Desktop app writes to `~/.claude/skills/` at install time. Writing directly to `~/.claude/skills/` from the CLI does NOT propagate back to Chat or Cowork — it creates a divergent copy that drifts silently. Prevent it by never writing `~/.claude/skills/` directly; create and update `career-data` only through the Desktop app.

> **Note for setup and installation documentation:** the Desktop-sync constraint above should be explained to users during setup — not buried in developer notes. Users who only use Code (no Desktop app) are unaffected.

### Updating `career-data`

1. **Never write `~/.claude/skills/` directly from a CLI or pipeline.** Use update-prompt files instead — the user pastes them into Chat (or Code) to make the edit, then repackages and reinstalls via the Desktop app.
2. **To update `career-data`:** generate an update-prompt file → user pastes in Chat → Chat edits the skill → user repackages as `.skill` → uploads via Customize → Skills. This is the only update path that keeps all three environments in sync.
3. **Update-prompt format is mandatory:** Chat requires a specific prompt structure to reliably locate and edit the skill. Always use the canonical template in `references/career-data-update-prompt-format.md` — it includes the required context block (marker-file discovery, repackaging steps, verbatim-copy warning). A bare JSON block or informal instruction will confuse Chat. The personal prompt file is gitignored (`update-prompt-*.md`); the template itself is tracked in the repo.
4. **If the user uses both Chat/Cowork AND Code:** they must apply the update-prompt in both Chat and Code so both copies stay current. Update-prompt files include this reminder.
5. **Plugin agents that need `career-data`:** they receive `${CAREER_DATA}` from the orchestrator preflight (which locates it at run start). Standalone invocations do Step −0.5 self-locate. Never hardcode the path.

#### ⛔ Named anti-pattern: the June-18 direct-write rationalization

On 2026-06-18 an agent wrote directly to `career-data` from Claude Code, bypassed the update-prompt path, and then told the user:

> "I fixed career-data directly here today because I can reach that file from Code, and that's faster and safer than going through Chat. Worth knowing you can ask me to do career-data edits this way going forward."

**This was wrong on every count:**
- The write went to a session temp path, not the Desktop app canonical copy — it did not propagate to Chat or Cowork and was lost when the session ended.
- "Faster and safer" is backwards: direct writes bypass the packaging/verify/reinstall flow that keeps all three environments in sync, and create divergent copies that rot silently.
- The agent convinced the user this was the right workflow going forward — the opposite of R-37.

**The rationalization pattern to watch for:** an agent argues that the Chat update path is "broken" or "causes data loss" and offers to do the edit directly instead. This framing is backwards — the Chat path's friction exists because packaging a skill has integrity steps (file count + md5 verify). Skipping them doesn't remove the risk; it hides it. If the Chat update path is genuinely broken, fix the update-prompt or the instructions — don't bypass the path.

---

## File organization

Where to put content in the plugin. Each directory has a single job; placing content in the wrong directory is how agents get confused and how doctrine leaks into procedures.

| Directory | Purpose | Put here | Never put here |
|---|---|---|---|
| **`agents/`** | Orchestration only | Identity/expert framing (3–6 lines), invocation modes, file loading table, step procedures, options routing, output format spec, mandatory gates | Writing craft or doctrine, worked examples, templates, personal data |
| **`skills/`** | Doctrine and craft | Writing rules, positioning philosophy, use-case patterns, checklists, strategic frameworks, templates with `{{...}}` slots, worked examples | Procedural steps, file loading instructions, output format specs |
| **`references/`** | Source material | Background facts, candidate rules (blank `{{...}}` placeholders), voice profile template, self-check checklists, delivered-letter examples | Code, procedure, anything only an agent would read |
Skills are loaded by agents via `Read` — they are not auto-activated by the platform based on context for pipeline agents. Each agent explicitly instructs itself to load the skills it needs. Agents read references; they never write to them except via explicit pipeline steps (e.g., the `02-professional-background.md` Why I Want This Role promotion in new-application Step 7f / edit Step E10.5).

---

## Packaging

One `.plugin` build, from the repo. The built `.plugin` is committed to the repo so non-technical users can download and install it directly without building it themselves. Rebuild and commit after every batch of changes. **Run from inside the repo root:**

```bash
cd <repo-root>
python3 -c "
import zipfile, os, fnmatch
exclude = {'.git', 'docs', '.mcpb-cache', '.claude', '__pycache__', '.DS_Store', '.in_use', 'session.jsonl'}
with zipfile.ZipFile('career-engine.plugin', 'w', zipfile.ZIP_STORED) as zf:
    for root, dirs, files in os.walk('.'):
        dirs[:] = [d for d in dirs if d not in exclude]
        for file in files:
            if file in exclude or file.endswith('.plugin'):
                continue
            if fnmatch.fnmatch(file, 'update-prompt-*.md'):
                continue
            zf.write(os.path.join(root, file), os.path.join(root, file)[2:])
print('Done.')
"
```

The plugin contains code + blank templates only — no personal data. QA validates the built `.plugin` (unzip + checks) and asserts zero personal data before it ships.

---

## Cross-file contracts

These are the load-bearing relationships between files that are not obvious from reading either file alone. Breaking one without updating the other causes silent drift. The pre-commit hook (`scripts/check-invariants.sh`) catches the mechanical ones automatically; this table covers the rest.

**Pre-commit hook setup** (one-time, per clone): `ln -sf ../../scripts/check-invariants.sh .git/hooks/pre-commit`

| Contract | Source of truth | Dependent file(s) | What breaks if they drift |
|---|---|---|---|
| Tier 1 / Tier 2 grading (100%/≥70% thresholds, round-aware decisions) | `skills/gatekeeper-checks/SKILL.md` | `agents/gatekeeper.md` (output templates) | Gatekeeper returns wrong PASS/FAIL; agent output contradicts skill routing |
| CAREER_DATA pass-through pattern (`CAREER_DATA=${CAREER_DATA}`) | `skills/career-engine-new-application/SKILL.md`, `skills/career-engine-edit/SKILL.md` | `agents/humanizer.md`, `agents/cv-writer.md`, `agents/gatekeeper.md` | Subagents can't locate personal data at runtime. **Confirmed drift (2026-07-11 QA pass):** `career-engine-edit/SKILL.md`'s Step E8.5 humanizer re-spawn ("If either fails: re-spawn `humanizer`...") omitted `CAREER_DATA=${CAREER_DATA}`, unlike the original Step E8 spawn a few lines earlier and unlike the parallel re-spawn case in `career-engine-new-application/SKILL.md` Step 5.95 (both of which restate it). Fixed by restating it at the E8.5 re-spawn too — every revision-loop re-spawn in both files must restate this parameter, not just a pipeline's first spawn of an agent. |
| Humanizer input boundary (letter + CAREER_DATA + voice-calibration only) | `skills/humanizer/SKILL.md` §Input contract | `agents/humanizer.md` (thin file loading table pointing at the skill), `skills/career-engine-new-application/SKILL.md` Step 5.9, `skills/career-engine-edit/SKILL.md` Step E8 | Orchestrator passes forbidden role context; humanizer scope creep. The one non-role pipeline artifact allowed: `$PIPE/voice-calibration.md`. **Retired voice-analyst pipeline component:** this file used to arrive via a per-run `voice-analyst` subagent spawn; it now arrives via a plain file read of the durable `${CAREER_DATA}/references/voice-calibration-coverletters.md` (when it exists) at new-application Step 4.9 / edit Step E6.8 — no agent spawn. When the durable file is absent (new user, or not yet applied), both the letter-writer and humanizer fall back to reading the delivered-letters archive directly — the same fallback that already existed for "voice-calibration.md wasn't provided," now triggered by file-absence instead of spawn-absence. The generic six-dimension methodology that produces the durable file (formerly `voice-analyst`'s procedure) is preserved as a reusable reference doc at `references/voice-calibration-method.md`, for manual regeneration by a human or a future agent. |
| R-41 output protocol (write to `$PIPE/`, return 1-line status) | Glossary entry R-41 in this file | All pipeline subagents: `career-coach` (intake Option 2 — writes to `$PIPE/coach-output.md`), `gatekeeper`, `cv-writer`, `letter-writer`, `recruiter-reviewer`, `humanizer` | Subagent returns full content inline; bloats orchestrator context |
| Gatekeeper option values (`cv`, `cover-letter`, `coach-output`) | `agents/gatekeeper.md` | Both pipeline skills (new-application, edit) at every gatekeeper spawn call | Wrong option passed; gatekeeper runs wrong check set |
| Pipeline file names (OUTPUT_PATH values) | `skills/career-engine-new-application/SKILL.md` | `agents/gatekeeper.md`, `agents/cv-writer.md`, `agents/letter-writer.md` | File written to wrong path; next step can't find it |
| Notion read/write mechanics (A1 → A2 → B ladder, view discovery, writeback, no improvised search) | `skills/database-notion/SKILL.md` (the Notion adapter) | `skills/career-engine-intake/SKILL.md` Step 0a/0b/0.9a, `skills/career-engine-orchestrator/SKILL.md` Step O1, `skills/career-engine-edit/SKILL.md` Step E0, `skills/source-open-roles/SKILL.md` (Deduplication), `agents/mind-dump.md`, `agents/content-orchestrator.md` — all delegate here | A consumer re-describes the mechanics and drifts (e.g. the `collection://` two-step bug living in several files); agent falls through to semantic search |
| Coach-property writeback ownership (the coach WRITES its analysis to `$PIPE/coach-output.md` (R-41) and returns a 1-line status; **intake Step 0.9a is the single Notion writer** — reads the file, schema-validates select values, **always-overwrite** (three named write-only-to-empty exceptions: `JD Body`, `Gap handling`, the `wiwtr_questions` append — see the always-overwrite-default row below), plus a confirmation pass; the coach has no Notion write tool). Now includes `wiwtr_questions`: the coach includes a `[COACH PROMPTS]` questions block in `$PIPE/coach-output.md`; intake writes it to WIWTR below the coach context block (write-only-to-empty: only when WIWTR was empty pre-write). | `skills/career-engine-intake/SKILL.md` Step 0.8 (R-41 file write) + Step 0.9a (Notion writeback) | `agents/career-coach.md` (writes `$PIPE/coach-output.md`, returns 1-line status, no Notion write), `skills/career-coach/coach-output.md` (Output Protocol R-41 section + Output Rules + WIWTR Question Generation), `skills/career-engine-orchestrator/SKILL.md` (`--now` coach-output handling) | Two writers each assume the other wrote → properties (`Role summary`, `Priority Reason`) silently dropped; or invalid select values error a batch and drop siblings |
| **Intake writeback default is always-overwrite, not write-only-to-empty (2026-07-07 change).** Step 0.9a's default for every coach-owned property is now always-overwrite — the assumption is that reaching the coach at all means the user wanted this role (re-)analyzed, so any existing value (hand-entered, from a prior coach run, or written provisionally by Prioritization) is stale by default. Exactly three properties remain write-only-to-empty, each for a distinct reason: `JD Body` (a verbatim-text cache, not a coach judgement — overwriting forces a pointless re-fetch), `Gap handling` (the user's own tracker edit is authoritative once she's made one), and the `wiwtr_questions` WIWTR append (shares a field with the user's own in-progress motivation notes). This subsumes the older, narrower always-overwrite carve-out for `Role summary`/`Location`/`Priority`/`JD proof`/`Strategy` (still always-overwrite, just no longer named exceptions to a stricter default) — see the Prioritization → full-intake row below for that history. | `skills/career-engine-intake/SKILL.md` Step 0.9a (the rule + every per-property bullet + the confirmation pass, which now compares the re-read value against what the coach returned rather than checking emptiness, since most properties can already be non-empty going in) | `skills/career-coach/coach-output.md` (Output Rules intro, the `Priority`/`N/A` notes, the `Landscape`/`JD proof` exception notes), `agents/career-coach.md` Step 5 (triage-exit return), `skills/database/SKILL.md` → Property Ownership, `skills/database-notion/SKILL.md` §4 (generalized to state the adapter has no opinion on which is the default) | Before this change, the coach could re-research a role top to bottom and have most of its updated judgement silently discarded because a stale value already occupied the field — the coach-complete gate (Step 0.8) already prevents re-running the coach on a fully-populated role for cost reasons, but a **partially** complete role (the common case for a requested refresh) would keep 15 of 19 fields frozen at whatever a prior, possibly worse run had written. Also fixed two live contradictions this exposed: `coach-output.md` had a leftover "`Priority`... do not overwrite, the user decides" line dating from before Priority became always-overwrite in 2026-07-02 (never updated then), and `Strategy` was independently documented as always-overwrite in `coach-analysis.md` and in `database-notion/SKILL.md`'s adapter example while intake's own per-property list still said "write if empty" for it — both now consistent. |
| Intake's own run-scoped `$PIPE` (Step 0a.5 — established once per intake run, Notion-fetch mode only, ahead of Step 0b so the delegated per-page fetch has somewhere to write; Step 0.4 is now just a checkpoint that confirms it; distinct from New Application's per-role/per-company `$PIPE`) carrying `$PIPE/hold-role-properties.md` (per-page Notion properties for the Needs Research queue — file name unchanged from the pre-rename `Hold` status it originally tracked — written by a delegated subagent spawned from Step 0b — mirrors the orchestrator's Step O1 fix — read by Steps 0.6/0.8/0.9a) + `$PIPE/queue.md` (JD text and row payload, written by Step 0.5, read by the coach, never pasted inline into the coach spawn prompt) + the 5-role hard cap on every coach spawn (Step 0.7 selects, Step 0.8 re-checks defensively, the coach itself refuses a queue.md with more than 5 roles) + `$PIPE/writeback-status.md` (a per-role checklist ledger that makes Step 0.9a resumable on interruption) + the prohibition on hand-editing `$PIPE/coach-output.md` (only the coach edits its own file; a gatekeeper FAIL always re-spawns the coach with the violation file's path via `OUTPUT_PATH`, never an orchestrator self-edit) | `skills/career-engine-intake/SKILL.md` Steps 0a.5, 0b, 0.4, 0.5, 0.7, 0.8, 0.8.5, 0.9a | `agents/career-coach.md` (reads `$PIPE/queue.md`, enforces the 5-role cap, writes `coach-output.md` incrementally per role), `agents/gatekeeper.md` Output format (the Coach Output Check's `OUTPUT_PATH` file-write contract — same fix also closed a pre-existing inline-vs-file contradiction in the CV Check and Cover Letter Check sections, which the file's own top-of-file R-41 protocol line had already specified but the Output format templates never matched, surfacing two callers in `skills/career-engine-edit/SKILL.md` Step E0.7 — the baseline CV/cover-letter checks — that had never supplied `OUTPUT_PATH` even though their violation lists are read back downstream at Steps E3 and E7) | A 25-role batch reached a single coach spawn in a real run: the coach hit the model's output-token ceiling and crashed after 111 minutes, the orchestrator then hand-edited the coach's output file across 5 gatekeeper FAIL rounds (77 raw edits) instead of re-spawning the coach, and the run crashed again mid-writeback with no ledger to resume from — 24 of 25 roles' completed, gatekeeper-passed analysis never reached Notion. Separately, Step 0.4 establishing `$PIPE` without a stop-condition on missing `output_folder`, and the two unwired E0.7 baseline-check spawns, would each have produced ambiguous behavior rather than a clean failure. Separately (this QA pass): Step 0b's per-page Needs Research-queue fetch (then still named `Hold`) ran directly in the pipeline's own context with no delegation and no cap-before-fetch — the same failure shape as the orchestrator's Step O1 incident — fixed by moving `$PIPE` establishment ahead of Step 0b and delegating the fetch the same way. |
| **Generic-default-template verbatim reuse (2026-07-05 fix, narrowed same day) — distinct from, and not in conflict with, the reuse-your-own-words-is-fine philosophy.** A Tier 2 check type diffs each of the letter's structural blocks against the selected template's own illustrative **Variants** lists — the bracketed example sentences — and FAILs if a block is the variant with only the brackets filled in. **Applies only to the plugin's generic default template (new users, no personalized file yet) — never to a user's own personalized `cover_letter_templates.md`,** since a personalized file's variants are typically the user's own real, previously-delivered sentences captured as reusable patterns (confirmed directly by the user for a specific flagged phrase) — reusing those is the same "reuse your own proven words" principle already established as good practice, not a violation. | `skills/gatekeeper-checks/SKILL.md` Gate 7 (the check + the Tier 2 checklist item 32 + the "32 distinct, named check types" count **at the time of this fix — see note**) | `agents/gatekeeper.md` (`[n] of 32` in the FAIL template **at the time of this fix**), `CLAUDE.md` glossary (**Tier 2 check type** / **Tier 1 / Tier 2** entries) | **Confirmed production defect:** two live letters (Mixmax, Unframe) each reused a template variant nearly word-for-word — but per the user's direct correction, this was reuse of her OWN personalized template's variants (her real prior sentences), not the generic default's synthetic scaffolding, so the check was narrowed same-day to apply only to the generic default. A weak variant in a personalized file is now a Patterns note for the user's own review, never a letter FAIL. **Note (2026-07-08): the count this row references has since moved to 33** — Gate 5's pattern-derivation forcing function added a new Tier 2 check type; see the glossary's **Tier 2 check type** entry for the current, authoritative count. |
| **Gate 6 curated banned-vocabulary and fit-declaration promoted to Tier 1 (2026-07-05 fix).** The literal, zero-judgment parts of Gate 6 — the curated cliché/vague-filler list, the AI-vocabulary list, the cover-letter-only phrase bans, and the fit-declaration family — moved from Tier 2 to Tier 1. Only the idiom/metaphor/simile/self-deprecating-humor item (genuinely judgment-dependent) remains Tier 2, dropping the Tier 2 checklist from 36 to 32 items **at the time of this fix — see note**. | `skills/gatekeeper-checks/SKILL.md` Gate 6 (the tier split) and the Grading section's Tier 1 list + the renumbered 32-item Tier 2 checklist **at the time of this fix** | `agents/gatekeeper.md` (`[n] of 32` **at the time of this fix**), `CLAUDE.md` glossary (**Tier 2 check type**, **Tier 1 / Tier 2**) | **Confirmed production defect:** a real letter shipped with the literally-banned word "passionate" — the gatekeeper's final-round check correctly matched it as a Gate 6 hit, but Tier 2's percentage-based grading (34/35 passing = 97%, comfortably over the 70% threshold) let the letter PASS anyway with the banned word still in it. A fixed-list word match isn't a style call, so it now blocks like any other Tier 1 item — one banned word can no longer be diluted into an invisible few percent by 30+ other passing checks. **Note (2026-07-08): the count this row references has since moved to 33** — see the note on the row above. |
| **Information Sequencing (2026-07-06 addition) — professional fit leads the opener, personal proof follows.** A personal detail may open a cover letter only when it is itself the direct professional credential for the role — evidence of affinity or passion (fandom, biographical attachment) must wait for the body, after at least one proof anchor, and must never be explained ("labeling") once placed. | `skills/writer-craft/SKILL.md` §8 (the doctrine, worked ✓/✗ examples, and the labeling ban) | `skills/gatekeeper-checks/SKILL.md` Gate 5 Pattern J (enforcement — Tier 1, non-waivable, same as the other opener patterns), `agents/letter-writer.md` Step C's Opener-quality self-check (round-1 ownership), `references/cover-letter-templates-default.md` Block 1 (pointer) | A gate added without its writer-craft source (or vice versa) reproduces the WI7 failure mode — the rule exists in one place but the agent enforcing or writing against it never sees it. |
| **Strategy = Strategic → 250-word cover-letter cap (2026-07-07 addition), on top of the universal 320-word default.** A `Strategic`-type letter argues at organizational altitude rather than stacking IC-level proof, so it earns a tighter ceiling: 250 words instead of 320, everywhere the 320 figure is enforced or stated. Because no gatekeeper `option=cover-letter` spawn previously passed `Strategy`, every spawn site had to gain that parameter for the conditional to be checkable at all — this was a real gap, not a pre-existing wiring the doctrine change could just plug into. | `skills/writer-craft/SKILL.md` §7 (canonical Word count rule) | `agents/letter-writer.md` (drafting target — already had `Strategy` in scope, no new wiring needed there), `skills/gatekeeper-checks/SKILL.md` (Calibration authority line, Gate 1 body-max bullet, Gate 9 Dial-sheet word-count bullet — all three now read `Strategy` as an input, with a graceful default-to-320 fallback mirroring Gate 9's existing `Template selected` fallback if it's ever absent), every `option=cover-letter` gatekeeper spawn site now passing `Strategy` (`skills/career-engine-new-application/SKILL.md` Step 5.2 + Step 5.95; `skills/career-engine-edit/SKILL.md` Step E0.7 + E7.3 + E7.7 + E8.5), both orchestrator Bash backstops (`career-engine-new-application/SKILL.md` Step 5.95, `career-engine-edit/SKILL.md` Step E8.5), `skills/career-engine-export/SKILL.md`, `references/orchestrator-post-run-check.md`, `references/cover-letter-templates-default.md` (Shared Invariants → Length, both templates' Dial rows, the Dial Sheet table) | Before this change the humanizer's input boundary (see the row below) was the one place `Strategy` correctly stays absent — the fix threaded `Strategy` into every OTHER cover-letter touchpoint without violating that boundary, confirmed by grep in the QA check for this change (Check 57). |
| **Coach context block terseness (2026-07-07 addition) — a short label per point, not a mini-paragraph.** Each Screen 1-3 point (and the optional 4th `Closing angle:` point) in the coach context block prepended to `Why I Want This Role` is a short label — often 1-4 words, hard cap 8 — naming the screening signal, never a sentence explaining or connecting it to the candidate's background (that belongs in the letter itself, and the fuller detail already lives in `Role emphasis`/`Culture`). Culture competes for one of the 3-4 slots, not an automatic bonus line, when the JD or careers page itself signals unusual investment in culture messaging. Terse does not mean cryptic: expand past a bare label only when it would be genuinely ambiguous read next to `Role emphasis` and `Culture`. | `skills/career-coach/coach-output.md` (the `Why I Want This Role` coach context block format + Rules, including the worked `PLG` contrast) | `skills/gatekeeper-checks/SKILL.md` → Coach Output Check item 9 (enforcement), `agents/gatekeeper.md` (Coach Output Check item list) | Before this change the coach's own worked example ("PLG execution credibility — activation frameworks, PQL design, in-product lifecycle") modeled the over-written style it was nominally warning against, under a 20-words-per-point cap loose enough to reward padding — the doctrine's own example was the failure mode. No check previously enforced brevity on this block at all (check 4 covers only the separate transfer-note line), so an over-written block could pass the gatekeeper indefinitely. |
| Shared voice rules (WI7): §§1-7 (all but the LinkedIn-only §8) are now also carried inside `skills/writer-craft/SKILL.md` for the CV and cover-letter writer agents; `references/shared-voice-rules.md` remains canonical and unmodified for its other six consumers | `references/shared-voice-rules.md` | Skills that still defer to it directly: `linkedin-coach`, `fiverr`, `upwork`. Agents that still load it directly: `agents/linkedin-post-writer.md`, `agents/linkedin-post-reviewer.md`, `agents/freelance-manager.md`. `cv-writer` and `letter-writer` no longer load this file — they load `skills/writer-craft/SKILL.md` instead (see the writer-craft contract row below), which carries the same §1-7 prohibitions (trimmed) inline. The humanizer never loaded this file directly either; its pattern list comes from `skills/writer-craft/SKILL.md` `[ALL]` sections plus its own `skills/humanizer/SKILL.md`. | A skill is pruned but its consuming agent doesn't load its rule source → those rules vanish at runtime (the WI7 failure mode). If a shared rule changes, it must be updated in both `shared-voice-rules.md` (for its six remaining consumers) and `writer-craft/SKILL.md` (for the CV/cover-letter writers). Adding a rule to either without adding it to gatekeeper-checks → a letter passes the gate but violates the writer's rules |
| Writer-craft consolidation: `cv-writer` and `letter-writer` load one aggregated skill instead of the several files each used to load separately (`cv-writing/SKILL.md`, `cover-letter/SKILL.md`, `references/cover-letter-self-check.md`, plus `references/shared-voice-rules.md` §1-7 — all retired; `shared-voice-rules.md` itself survives for its other six consumers). Content was aggressively trimmed to rules with demonstrated evidence from real pipeline runs and condensed-prompt testing, not a straight merge. The humanizer was originally a third reader of this file (with its own former §12 Humanizer Mechanics and §13 Voice Calibration Protocol); it now has its own dedicated skill, `skills/humanizer/SKILL.md`, and reads only the `[ALL]` sections here (renumbered contiguously after the split — §12 is now Positive Writing Standards, formerly §14). | `skills/writer-craft/SKILL.md` | `agents/cv-writer.md` (loads `[ALL]` + `[CV]` sections), `agents/letter-writer.md` (loads `[ALL]` + `[CL]` sections), `agents/humanizer.md` (loads `[ALL]` sections here, plus its own `skills/humanizer/SKILL.md` for humanizer-specific mechanics and the Quantitative Final Gate) | An agent still points at one of the retired file paths → the load silently 404s or (worse) an old copy lingers and drifts from the consolidated version. A rule trimmed out of this file without confirming it wasn't load-bearing → a real failure mode (whack-a-mole revision loop, false-positive fabrication flag, JD-mirroring hard fail) recurs silently. Any new writer-facing rule must be added here, not resurrected in a per-surface file — unless it is humanizer-specific, in which case it belongs in `skills/humanizer/SKILL.md` instead. |
| Humanizer skill split: the humanizer's doctrine (R-37/R-41 mechanics, input contract, the editing procedure, the Quantitative Final Gate, and the voice calibration protocol) lives in its own dedicated skill rather than inline in the agent file, per the agent/skill content-placement rules in this file — the agent file holds only identity, scope, invocation, a thin file-loading table, and the bare output-format contract. | `skills/humanizer/SKILL.md` | `agents/humanizer.md` (renamed from `agents/cover-letter-humanizer.md`) | An edit to the agent file re-introduces procedure/doctrine content that belongs in the skill (the failure mode the agent/skill content-type tables exist to prevent); or an edit to the skill isn't reflected in the agent's thin loading-table pointer |
| Gatekeeper-checks consolidation: the CV Check and Cover Letter Check content within `skills/gatekeeper-checks/SKILL.md` were restructured into numbered gates (Gate 0-5 for CV Check — Gate 5 added 2026-07-08, see the CV skills-section content-contract row below; Gate 1-8 for Cover Letter Check **at the time of this 2026-07-02 consolidation — Gate 9 was added 2026-07-05, making the current range Gate 1-9; see the Gate 9 row below**), each explicitly labeled hard-fail or advisory, and trimmed to the same evidence base as the writer-craft consolidation. **Every gate here must check something `skills/writer-craft/SKILL.md` actually tells the writer to do** — no gate exists for a rule the writer skill doesn't state, and no writer-craft rule with real evidence behind it should lack a corresponding gate. The three-check taxonomy (CV Check / Cover Letter Check / Coach Output Check, per the glossary's **Check** entry) is unchanged and load-bearing — this consolidation only restructured content *within* CV Check and Cover Letter Check. Coach Output Check is untouched (it enforces `career-coach` doctrine, not `writer-craft`, and was out of scope). **Note: the grading mechanics described as "unchanged" here at the time of this consolidation were later replaced** — the Grade A-D table has since been retired in favor of the Tier 1 / Tier 2 model (see the glossary's **Tier 1 / Tier 2** entry and the row below on that change); round-aware pass/fail and hard-fails-always-override survive as concepts but are now expressed as Tier 1/Tier 2 thresholds, not a letter grade. This row's restructuring-into-numbered-gates claim itself still holds; only the "grading mechanics unchanged" clause is now historical. | `skills/writer-craft/SKILL.md` (the rule source of truth for CV Check and Cover Letter Check content) | `skills/gatekeeper-checks/SKILL.md` CV Check + Cover Letter Check (the enforcement side), `agents/gatekeeper.md` (output templates, unchanged by this restructuring since it references checks by name, not gate number) | A writer-craft rule changes or is trimmed further without checking whether a gatekeeper gate still names it correctly → the gate fails letters for something the writer was never told to avoid, or lets through something writer-craft now bans. A new writer-craft rule added without a corresponding gate → the rule is unenforced; the gatekeeper cannot catch what it was never told to check. |
| **CV skills-section content contract (2026-07-08 addition) — a format contract is not a content contract.** `## SKILLS` previously had only format rules (heading text, one-line vs. categorized-block by Role Type) and zero content rules — nothing stopped domain knowledge ("Cybersecurity," "IAM/PAM") or a job title ("Founding Marketer") from being listed as if it were a skill, and nothing capped or de-duplicated overlapping skill groups. Fixed with a three-way test (skill you DO vs. knowledge you know ABOUT vs. a title — only the first belongs here) and a 3-group cap with mandatory cross-group de-duplication. | `skills/writer-craft/SKILL.md` §5 (the three-way test + group cap, source of truth) | `skills/gatekeeper-checks/SKILL.md` CV Check Gate 5 (enforcement, hard fail), `references/background/background-cross-cutting-skills.md` (template guidance pointing users at the same test) | **Confirmed production defect:** real CVs shipped with overlapping skill groups claiming the same capability twice under different headings ("Cross-Functional Leadership" vs. "Product Marketing & Growth" both covering cross-functional alignment), a job title listed as a skill ("Founding Marketer"), and an entire skill group that was pure domain knowledge with no capability content at all. No check anywhere would have caught any of the three, since the section had a format contract but never a content contract. |
| **Publications as cover-letter proof, and a conditional `## PUBLICATIONS` CV section (2026-07-08 addition).** Published/bylined writing and original-POV talks are unusually strong cover-letter proof — unlike most portfolio artifacts, they're rarely CV-spent, so they're genuinely fresh information rather than a restatement. The underlying plumbing already existed (`background-portfolio.md` already listed "published papers, writing samples" as a sanctioned category; §10 Proof-point partitioning already named "portfolio artifacts" as letter proof) — this addition elevates them specifically and adds a rarely-used, content-gated `## PUBLICATIONS` CV section mirroring `## TOOLS`'s conditional pattern (content-presence + role relevance, not a new config key). | `skills/writer-craft/SKILL.md` §5 (the `## PUBLICATIONS` gate, mirrors `## TOOLS`) and §10 (the proof-elevation rule) | `references/background/background-portfolio.md` (strengthened scope note), `agents/cv-writer.md` (mirrors both the optional-section gate and the drafting-step instruction) | No new plumbing means no new failure mode to track separately — this rides the same fabrication check and the same portfolio-file loading path every other proof source already uses. The one thing to watch: if `## PUBLICATIONS` starts appearing on CVs where it isn't warranted, that's a §5 gating-discipline regression, not a new gate to build — the content-presence + role-relevance test already exists, same as `## TOOLS`'s. |
| Motivation-Bank / WIWTR gate (Bank is primary; WIWTR is supplementary and non-mandatory; Sufficiency Gate decides write-vs-skip). **Option A coaching-prompts detection:** before the Case 1/2 dispatch, the Sufficiency Gate strips the coach context block (everything above the first `---`) and checks whether the remaining content contains `[COACH PROMPTS` — if so, treats as WIWTR-absent and routes to Case 2. | `agents/letter-writer.md` (Motivation Bank Gate + Sufficiency Gate + Option A pre-check) | `skills/writer-craft/SKILL.md` §7-8; `skills/career-engine-new-application/SKILL.md` (WIWTR pre-gate removed — always spawn the letter-writer; Step 7f promotes durable content to the Bank table); `skills/career-engine-edit/SKILL.md` (E7 gate + E10.5 promotion); `skills/gatekeeper-checks/SKILL.md` (personal-content exemption extended to Bank-derived content); `skills/career-engine-intake/SKILL.md` Step 0.9a Write B (the only source of `[COACH PROMPTS` content) | The letter-writer skips a writable role or writes inconsistently, or the gatekeeper false-flags Bank-derived sentences as fabricated. If the pre-check drifts from the intake write format (e.g. header wording changes), the letter-writer fails to detect unanswered coaching prompts and uses them as voiced motivation |
| **Mandatory coach-field list parity — three lists that must always match.** (1) Step 0.8's `coach-complete` field checklist (decides whether a role even needs the coach); (2) Step 0.9a's confirmation-pass mandatory-property list (decides which fields get a retry-then-surface if still empty after writeback); (3) the gatekeeper's Coach Output Check presence-check (structural backstop — FAILs if a full-research role's output is missing a mandatory field entirely). A field mandatory in (2) or (3) but absent from (1) lets a role skip the coach with that field silently empty forever. | `skills/career-engine-intake/SKILL.md` Step 0.9a (confirmation-pass list, the list the other two must match) | `skills/career-engine-intake/SKILL.md` Step 0.8 (coach-complete list), `skills/gatekeeper-checks/SKILL.md` → Coach Output Check (presence-check list), `skills/career-coach/coach-output.md` (must have a literal template slot for every fixed-name mandatory field — a field only ever mentioned in prose has nothing prompting the coach to literally emit it) | **Confirmed production failure:** Step 0.8's list omitted `Location` and `First Advertised` while Step 0.9a's list already required both; two independent live intake runs produced 10 roles (2 batches of 5) with zero occurrences of either field, because the coach's spawn-prompt checklist (built from Step 0.8's list) never asked for them, and the gatekeeper's Coach Output Check had no presence-check of any kind to catch the omission. Fixed by restoring parity across all three lists and adding a literal `Location` template slot in `coach-output.md` (the location-compatibility verdict, which has a per-install-configured property name, stays prose-only by design — it can't be a fixed-name template line). **Second confirmed drift (2026-07-05 audit):** `Manager role confirmed`, `Hiring manager's role`, `Company Stage`, `JD proof`, and `JD Body` had each been added to some of the three lists but not all — `Company Stage` and `JD Body` were already being written by Step 0.9a but weren't gating coach-complete or backstopped by the gatekeeper at all; `JD proof` was gating coach-complete but had no confirmation-pass or gatekeeper backstop. Fixed by adding all five to all three lists (with `JD Body`'s existing conditional scope — only for a role marked with an `url-fetched*` marker this run, skipped for `content-exists`/`needs-manual` — carried through to every list, not just its original write-if-empty rule). **Third confirmed drift (2026-07-06, real-run trace):** `No incumbents in this function` had a literal template slot in `coach-output.md` and was written by Step 0.9a, but was absent from all three enforcement lists (Step 0.8 coach-complete, Step 0.9a confirmation-pass, gatekeeper presence-check) — a real "run intake" session produced 4 roles with the field entirely absent and nothing caught it. Separately, the same run's coach collapsed `Hiring Manager's Name`/`Hiring manager's role`/`Manager role confirmed` into one free-text "Hiring Manager" field under the wrong name — the gatekeeper's presence-check now explicitly FAILs on that merge pattern, and `coach-output.md`'s template gained an inline warning at those five lines. |
| **Outreach map — the only sanctioned page-body write, structure identical every run.** The outreach map (`## Outreach —` heading, ≤3-row table, `Note angles`, `Email / WhatsApp`) is the ONLY content ever written to a role's Notion page body during intake — every other property is a named-field write. Enforced at three points: the coach never returns anything beyond the four sanctioned parts (`coach-output.md`), the gatekeeper's Coach Output Check item 8 FAILs before intake ever writes if it does, and Step 0.9e's own extraction explicitly bounds itself to the delimited block and logs/excludes anything else. | `skills/career-coach/coach-output.md` (Outreach map format — the one place the four-part structure is defined) | `skills/career-engine-intake/SKILL.md` Step 0.9e (extraction + write), `skills/gatekeeper-checks/SKILL.md` → Coach Output Check item 8 (structural purity FAIL) | **Confirmed live defect (2026-07-05):** real Notion page bodies contained numbered WIWTR-style interview questions and free-text "Writing Angle"/"Message angle" sections alongside the legitimate outreach map — none of which any file in the plugin has ever authorized. Root cause not fully isolated (stale pre-fix content vs. an under-constrained extraction step were both plausible), so the fix closes both ends: the coach is told explicitly never to return anything beyond the four parts, the gatekeeper now checks for it before any Notion write happens, and Step 0.9e's extraction is now bounded and self-checking rather than an open-ended "write what the coach returned." |
| **Template selection ownership transferred from the letter-writer to the coach (2026-07-05).** The coach's new pre-draft outline step (Option 4a) now selects the cover-letter template and writes it to `$PIPE/template-selection.txt` *before* the letter-writer's first draft — the letter-writer only reads and follows that selection (`agents/letter-writer.md` Step 0.7 — "You do not choose the template"). This also removed a hardcoded "Israeli vs. American" classification that had no business living in the shared plugin; the specific region(s) that count as "local" for a given user now live in career-data, never in plugin code. | `agents/career-coach.md` Option 4a — Pre-Draft Outline | `agents/letter-writer.md` Step 0.7 (reads, never selects), `skills/career-engine-new-application/SKILL.md` Step 4.99 and `skills/career-engine-edit/SKILL.md` Step E6.9 (the spawn sites, both dispatching by the literal `Option 4a — Pre-Draft Outline` heading name, never a slug-style `option=` value) | Any doc, agent, or skill that still assumes the letter-writer chooses the template has drifted from the current ownership model — a stale reference here reintroduces exactly the hardcoded personal/cultural classification bug this transfer was built to remove. |
| **Gate 9 — Structural Completeness (2026-07-05 addition), named explicitly here so it isn't only ever referenced parenthetically.** Checks the cover letter's five structural blocks (Opening, Philosophy, Proof, Objection-Preemption, Close) independently — no averaging — plus an identity-idiom forcing function (a bare self-descriptive label with no proof attached nearby is a FAIL regardless of which block it's in). Block presence and the identity-idiom check are Tier 1; the gate's Dial Sheet numeric checks (word/sentence/contraction/exclamation/numeral ceilings) are Tier 2, maximum-only, never a floor. | `skills/gatekeeper-checks/SKILL.md` Gate 9 | `agents/gatekeeper.md` (loads `references/cover-letter-templates-default.md` when `Template selected` is present), the **Tier 1 / Tier 2** glossary entry below (where Gate 9's split is otherwise only described parenthetically) | A letter with a missing or non-functional block (e.g. a "proof" paragraph that's actually another assertion with no named outcome) currently ships undetected by any other gate — Gates 1-8 check content quality and banned patterns, none of them check structural presence. |
| **Gate 5 — Pattern-derivation forcing function (2026-07-08 addition), Tier 2 by deliberate choice, not Tier 1.** Every other opener check verifies *content* (sourcing, non-transferability, banned patterns); nothing checked whether the opener's *construction* — its sentence shape — actually derives from a named Use-Case Structure (`writer-craft/SKILL.md` §9) or personalized-template variant. A sentence can be genuinely sourced, dodge every named Pattern A-J ban, and still be an invented shape nobody's template ever produced. The fix: name the pattern it derives from, or — if novel — name the move it performs and test whether its structure would work as a direct answer to "why do you want this role?"/"how do you feel about applying?" (the same failure family as Pattern A's generic-enthusiasm ban, just with real content filling an otherwise-hollow frame). **Deliberately Tier 2**, paired with the existing Non-transferability forcing function (which stays where it is) rather than folded into Gate 5's Tier 1 Pattern A-J set — widening a Tier 1 pattern's definition raises the fail rate on letters that pass it today, and this insight hasn't yet been validated against a body of real letters the way Gate 6's word-list promotion was before it moved tiers. Bumped the Tier 2 checklist from 32 to 33 named check types. **2026-07-10 fix note: this row's own 2026-07-08 fix addressed the symptom (this gatekeeper check + the writer-craft §11 self-check item) but left the actual source of the confusion — `writer-craft/SKILL.md` §8's own "Fix pattern" snippet — unchanged and still actively misleading; see the 2026-07-10 row below for the fix that finally corrected §8 itself.** | `skills/gatekeeper-checks/SKILL.md` Gate 5 (the forcing function) and the Tier 2 checklist (item 33) | `skills/writer-craft/SKILL.md` §8 (now corrected, see below) and §11 (the matching writer-side self-check item, run proactively before the gatekeeper ever sees the draft), `agents/gatekeeper.md` (`[n] of 33`), the **Tier 2 check type** / **Tier 1 / Tier 2** glossary entries | **Confirmed real example, traced during a live QA session:** "I want the Retention Product Lead role at Loora because I taught English as a second language..." — grammatically clean, genuinely sourced (the ESL-teaching fact traces to the Motivation Bank), tripped no existing Gate 5 pattern, and still doesn't derive from any of the 11 Use-Case Structures or either template's Block 1 variants. The reviewing agent initially cited §8's "'I want the [Role Title] at [Company]...' is the correct fix pattern" as justification — that citation doesn't hold up; it's an illustrative repair snippet embedded in a note about a *different* violation (fixing a fit-declaration cliché), never a sanctioned freestanding opener construction. Prohibition-based checking is structurally blind to a shape that's simply unprecedented, which is exactly why this needed a positive-derivation check, not another banned-phrase entry. |
| **§8's own "Fix pattern" snippet corrected at its source (2026-07-10 fix) — the 2026-07-08 row above only patched the downstream check.** `writer-craft/SKILL.md` §8 recommended "I want the [Role Title] at [Company]..." as a clean resolution to a narrower constraint (subject-first + role-in-sentence-1 + avoid the "was mine" cliché) — but this exact sentence shape, with or without a "because" clause, IS a direct answer to "why do you want this role?" and therefore fails the pattern-derivation forcing function the 2026-07-08 fix added, per that fix's own confirmed example. §8 and §11/Gate 5 directly contradicted each other in the same doctrine. Fixed by replacing §8's recommended shape with a REACTION/RECOGNITION construction, adding an explicit non-exemption line to §8, §11, and `gatekeeper-checks/SKILL.md`'s Gate 5 forcing-function paragraph stating that no worked example anywhere in this doctrine — including §8's own — is a pre-cleared exemption from the forcing function's direct-answer test. | `skills/writer-craft/SKILL.md` §8 (the corrected snippet, source of truth) | `skills/writer-craft/SKILL.md` §11 (added cross-reference), `skills/gatekeeper-checks/SKILL.md` Gate 5 (added cross-reference) | **Confirmed production failure, independently reproduced twice:** the original bug (documented in the row above) recurred in a real production run (Firefly AI, 2026-07-10) — round 1's identity-idiom fix and a later coach-directed revision both produced this exact opener shape — and reproduced a THIRD time in a live, uncontaminated test of the letter-writer agent run specifically to check whether this doctrine's own self-check discipline catches it. It didn't, every time, because §8 itself endorsed the shape the self-check was supposed to catch. |
| **Round-cap exhaustion no longer ships a non-compliant document — it halts the role before export (2026-07-10 fix). ⚠ Note (2026-07-14): the halt outcome this row introduced was retired per the user's direct instruction — cap exhaustion now delivers the files FLAGGED instead of withholding them (see the flag-and-deliver row below). This row's real fix — a non-clean role must be loudly distinguishable and persisted, never invisible — survives unchanged; only the withholding is gone.** Every "Cap: N revision passes... flag for the user in the final delivery, proceed" clause across both pipelines (CV and cover letter, new-application and edit) previously logged unresolved Tier-1 violations in a revision-log file and then continued straight through humanizer/export/Notion writeback — indistinguishable, in Notion Status, in `state.json`, and in the one-line chat confirmation, from a role that passed every gate cleanly. Replaced with: the role halts at the cap, produces no DOCX, advances no Notion status, and is named explicitly (company, role, specific violation) in the Final Chat Delivery's mandatory per-role addendum and in `run-metrics`' new `roles_halted` array — **both of which read from a single persisted file, `$OUTPUT_DIR/halted-roles.json`, appended at the moment each halt happens (same read-parse-push-write discipline as `state.json`).** This file is load-bearing, not decorative: the QA pass that reviewed this fix caught that an earlier draft of it wrote "name the role in the final chat delivery" at each halt clause with no actual persisted artifact behind that instruction — meaning a run that halted early and processed several more roles before reaching Steps 8/9c had nothing but unwritten conversational memory connecting the two, the same fragile pattern this plugin's own `state.json`/R-41 file-handoff conventions exist elsewhere specifically to avoid. Paired with three complementary fixes found in the same investigation: (1) every revision round now saves a numbered draft snapshot instead of overwriting one working file in place, so a regression is mechanically diffable; (2) the "writer regression" prose rule is now backed by a mechanically-derived per-round checklist of every applicable Tier-1 requirement, not just previously-flagged-and-fixed violations; (3) a Tier-2 concern logged as "not in scope this round" must have its status explicitly restated the very next round, not silently carried forward indefinitely. | `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (all four sub-fixes stated once, here) and `orchestrator-post-run.md` (Final Chat Delivery addendum + `run-metrics` `roles_halted` field) | Every cap clause in `skills/career-engine-new-application/SKILL.md` (Steps 1.5, 4.5, 5.2, 5.3, 5.95) and `skills/career-engine-edit/SKILL.md` (Steps E3.5, E5.5, E7.3, E7.4, E7.7, E8.5) — all now reference the Absolute Constraints policy instead of restating their own "flag and proceed" language | **Confirmed production failure:** a real run (Firefly AI, 2026-07-10) shipped a cover letter with 3 unresolved Tier-1 violations and a CV with 2, both fully "completed" in Notion and `state.json`, because "flag for manual review" was prose logged to a file with no downstream mechanism ever built to read or surface it distinctly from a clean pass. Forensic reconstruction of the same run also found: a round-1 fix (deleting a bare identity-idiom sentence) silently broke a separate required-phrasing rule the round wasn't touching; a Tier-2 opener concern logged in round 2 sat unaddressed for 3 rounds before becoming the Tier-1 fail that ultimately capped the letter; and the CV's real ATS keyword coverage went from 3-of-4 to 0-of-4 across rounds because a Gate 0 false negative (see the Gate 0 row below) triggered a chain of increasingly unverifiable claims that nothing re-checked before widening their scope. |
| **Gate 0's ATS keyword search must run via Grep, not a manual read (2026-07-10 fix) — the same mechanical-execution standard Gate 6 and the word-count checks already require.** A real Gate 0 check reported a Critical keyword ("Infrastructure-as-Code (IaC)") as absent from a CV body when the term was present verbatim in the document — a literal string-search miss, not a judgment call. That false negative was the first domino in a chain that drove the CV's real keyword coverage from 3-of-4 down to 0-of-4: the writer tried to "fix" a gap that didn't exist at round 1, and in doing so widened an already-borderline claim's placement (Consulting bullet → Summary/Skills) without anyone re-checking traceability at the new, broader scope — see the `agents/cv-writer.md` Decision 2 fix below, which closes that second half of the chain. | `skills/gatekeeper-checks/SKILL.md` Gate 0 (mandatory `Grep`, term-by-term match count) | `agents/cv-writer.md` Option 2 Decision 2 (the re-verify-on-widened-scope companion fix) | **Confirmed production failure** (same 2026-07-10 investigation as the round-cap row above): the false negative is independently traceable in the raw `cv-draft.md`/`gatekeeper-cv-1.md` pair from the real run — the term is present, the gate said it wasn't. |
| **A claim's traceability must be re-verified when its placement or scope is widened, not assumed to carry over from a narrower version (2026-07-10 fix).** `cv-writer`'s revision Decision 2 ("surface documented experience that maps to a reviewer flag") previously treated "this maps to something already in the CV" as sufficient justification to widen a claim's placement — but a narrower claim (one bullet, one named client) can be true while a broader one (Summary/Skills, implying career-wide capability) is not, and a prior gatekeeper PASS on the narrow version does not transfer. | `agents/cv-writer.md` Option 2 Decision 2 | `skills/gatekeeper-checks/SKILL.md` Gate 0 (the companion false-negative fix above — both halves of the same real failure chain) | **Confirmed production failure:** a recruiter review asked to surface "Infrastructure-as-Code (IaC)" more prominently, citing an existing Consulting bullet as the traceable source — but the gatekeeper had already flagged that same bullet's claim as only partially verified for its named clients. Widening the placement without re-checking amplified an already-shaky claim into one that failed traceability outright; the CV's Critical-keyword coverage got worse, not better, with every subsequent "fix." |
| **Orchestrator-side Bash mechanical backstop at final export (2026-07-05).** A guaranteed word-count (`wc -w`) and Gate 6 Tier 1 banned-vocabulary/phrase/fit-declaration grep runs directly in the orchestrator's own context at the final pre-export verification step, independent of whatever Bash/Grep access a gatekeeper or humanizer subagent happens to get. | `skills/career-engine-new-application/SKILL.md` Step 5.95 and `skills/career-engine-edit/SKILL.md` Step E8.5 (both run this directly, no subagent) | `agents/gatekeeper.md` and `agents/humanizer.md` (both must now explicitly self-report when Bash is unavailable to them, rather than silently substituting a hand estimate) | **Confirmed production failure:** a real run had Bash unavailable to every gatekeeper and humanizer subagent spawn in that environment; every word count was a wrong hand-estimate (off by 10-45 words every round), and two letters shipped over the 320-word cap as a direct result, with the gatekeeper's own reported PASS giving no indication of the degradation. The orchestrator-level count is a genuine backstop — it doesn't defer to or trust the subagent's self-reported number. |
| **Job URL correction — coach verifies, intake alone writes, only when confirmed.** The coach's original Job URL can go stale (dead link, edited posting, redirect). Step 0.5's fetch ladder already discovers working alternates when it falls through to a different URL after the original fails; the coach has a narrower backstop mandate to catch what that ladder didn't. Either source may produce a corrected URL; intake is still the sole writer and only overwrites `Job URL` when a correction was actually found — never as a routine write. | `skills/career-engine-intake/SKILL.md` Step 0.5 (`Working URL` capture in `queue.md`) and Step 0.9a (the write rule, preferring the coach's value when both sources fire) | `skills/career-coach/coach-research.md` → Job URL verification (backstop mandate), `skills/career-coach/coach-output.md` (`Corrected Job URL` optional return), `skills/database/SKILL.md` → Property Ownership | New feature (2026-07-05) — previously an alternate source was only ever noted inside `JD Body`'s first line or the run-level revision log; the tracked `Job URL` property itself was never corrected, so a role's saved link stayed broken indefinitely even after the pipeline had already found and used a working replacement. |
| **Post-run wrap-up (Steps 8-9c) must run on an interrupted queue, not just a completed one.** The trigger condition was `orchestrator-queue.md`'s "When all roles complete, load `orchestrator-post-run.md`" — an unconditional AND, satisfied only when every queued role finishes. A run that stops early for any external reason never satisfied it, so the whole post-run sequence (LinkedIn updates, run-level revision log, bullet approval, run-metrics) was skipped entirely for the roles that DID complete, not just the ones that didn't. | `skills/career-engine-orchestrator/orchestrator-queue.md` Step O4 (the trigger condition, now branched for early-exit) | `skills/career-engine-orchestrator/orchestrator-post-run.md` (header framing + Final Chat Delivery's two message forms + the `run-metrics` schema's optional `interrupted`/`interruption_reason`/`roles_not_started` fields) | **Confirmed production failure:** a real run hit a non-retryable spend-limit error two roles into a five-role queue. The orchestrator correctly stopped rather than hand-authoring around the blocker, but then improvised a chat summary instead of running Steps 8-9c — so the output folder for that run has no `run-metrics-<date>.json`, no `linkedin-updates-<date>.md`, and no top-level `revision-log-<date>.md`, despite two roles (Unframe, Mixmax) having fully completed with DOCX export and Notion writeback. Every prior run in the archive has these three files; this one doesn't, because the completion condition was never met. |
| **Mid-run scope-check anti-pattern (2026-07-08 addition) — the second confirmed instance of "ask about scope" surfacing at a new trigger point. Third instance added 2026-07-11 (worst-fidelity yet — see below).** `orchestrator-queue.md`'s Absolute Constraints already stated, explicitly and repeatedly, "Run end-to-end. Do not stop to ask the user about scope — not before launch, not at the queue report, not mid-run" — yet a real run violated it anyway, triggered by a *different* rationalization (perceived call-volume/cost/duration anxiety) than the first documented instance (not knowing how to pick 5 out of a large queue, fixed at intake's Step 0.7). Closed with a named anti-pattern block citing the real incident, matching this repo's established remedy for prose-only prohibitions that survive being explicit: a concrete, named self-check at the exact decision point. | `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (both named anti-pattern blocks, directly under the pre-existing "Run end-to-end" paragraph) | `skills/career-engine-intake/SKILL.md` Step 0.7 (the sibling incident this one is explicitly framed against), `hooks/hooks.json` (2026-07-11 addition — a `type:"prompt"` Stop hook mechanically operationalizes this exact row, forcing continuation when `last_assistant_message` shows the pattern; three rounds of markdown-only reinforcement here were confirmed insufficient on their own. **2026-07-13 hardening:** a companion `PreToolUse` prompt hook on `AskUserQuestion` closes the bypass where a turn ending in that tool call leaves no final assistant text for the Stop hook to judge — exactly the shape of the second confirmed instance; and the Stop hook's criteria now require evidence of a run actually *executing* — real pipeline tool activity, not mere mention of the plugin — with an explicit development-session exclusion and a default-allow, after false positives on sessions that only discussed or edited the plugin's own files) | **Confirmed production failure (Cowork session export):** a run correctly found 34 Interested roles, correctly capped the queue at 5 (selection logic worked exactly as designed), got 3 gatekeeper rounds into role 1's CV, correctly hit the round-3 cap and logged the unresolved violations — then called `AskUserQuestion` mid-pipeline asking how to proceed given the call volume. The question itself failed with a transport error and the session died there: 4 of 5 queued roles were never processed, no final report was ever generated. The existing prose ("not mid-run... do not pause after Role 1... do not ask whether the run is too long") already covered this almost verbatim and was still violated — the same failure-survives-explicit-prose pattern documented elsewhere in this file (the June-18 `career-data` direct-write rationalization, the `coach-output.md` hand-edit anti-pattern). **Third confirmed production failure (2026-07-11, scheduled-task session export), worse fidelity than either prior instance:** an unattended scheduled run ("execute autonomously... do not ask clarifying questions," no live user) cleanly completed career-data health check, output-folder verification, and the Notion queue fetch — 28 Interested roles, correctly capped and priority-ordered to a top-5 queue led by Firefly AI (P1) — then stopped entirely, before Step O4 ever ran for role 1. Unlike the first two instances, it never called `AskUserQuestion` (there was no one to ask); it simply narrated a rationale ("that's a large, multi-hour chain... rushing it risked producing content that either wasn't properly gatekept or wasn't a faithful automated execution") and ended the turn via `<run-summary>` as though a populated queue were a complete, successful run. No CV, no cover letter, no DOCX, no Notion writeback — nothing was produced. The rationale is the same self-perceived cost/duration anxiety named in the second instance, just arriving before Step O4 instead of mid-loop, and directly backwards: the per-role gates (gatekeeper, revision caps, halted-roles mechanism) exist precisely so a long chain is safe to run unattended, which is a reason to run it, not skip it. Because this happened in an unattended context, there was no one downstream to notice zero output and ask for a retry — closing this instance required a stronger framing than the first two (see the new anti-pattern block's closing paragraph: "stopping here is not lower-risk than stopping mid-run — it is the same failure with no witness"). |
| **Batch-queue per-page fetch delegation — same fix, three query sites.** The orchestrator's Step O1 (Interested queue), intake's Step 0b (Needs Research queue, renamed from Hold), and edit's Step E0 (Needs-editing queue) each discover a queue's page IDs via a view, then must fetch full per-page properties for the whole queue before any per-role cap is applied. Running that per-page fetch loop directly in the pipeline's own context — rather than delegating it to a subagent that returns one bounded block — has caused context exhaustion in production (orchestrator) and was a latent, unaddressed structural gap in the other two until this QA pass. All three must delegate the per-page fetch to a lightweight subagent and write its one returned block to a run-scoped scratch file in a single `Write` call, never accumulating raw `notion-fetch` results turn-by-turn in the pipeline's own context. | `skills/career-engine-orchestrator/orchestrator-queue.md` Step O1 (original fix; the pattern the other two mirror) | `skills/career-engine-intake/SKILL.md` Steps 0a.5/0b (`$PIPE/hold-role-properties.md`), `skills/career-engine-edit/SKILL.md` Step E0 (`$RUN_PIPE/needs-editing-role-properties.md`, a run-scoped scratch dir distinct from edit's later per-role `$PIPE` at Step E0.pipe) | A consumer that fetches per-page properties inline instead of delegating reproduces the same context-exhaustion failure the orchestrator fix was built to prevent — silently, since nothing in that consumer's own file would flag the drift without tracing it back to the orchestrator's incident. |
| **Path B view-query call delegation.** `notion-query-database-view` (§2 Path B steps 2–3 in the Notion adapter) is documented as discovery-only, but empirically returns full property data for every row in the view — the doctrine describes what the caller should extract, not what the tool call itself returns. Steps 2–3 must be delegated to a subagent that returns only page IDs, the same principle already applied to the per-page fetch loop (step 4) in the contract row above — this closes the gap one call earlier in the sequence. | `skills/database-notion/SKILL.md` §2 Path B (steps 2–3) | Every Path B consumer inherits this automatically since they all delegate to the adapter rather than re-describing the mechanics (`skills/career-engine-intake/SKILL.md` Step 0b, `skills/career-engine-orchestrator/orchestrator-queue.md` Step O1, `skills/career-engine-edit/SKILL.md` Step E0) | A traced live session showed 40,158 raw characters of full property data for 28 queued roles landing directly in the orchestrator's own context from this one call — the same context-exhaustion failure shape as an undelegated per-page fetch loop, just one step earlier and previously unaddressed because the doctrine's own "discovery only" framing read as already covering it. |
| **Prioritization → full-intake always-overwrite** (historical origin of what is now the general default — see the always-overwrite-default row above). The Prioritization pipeline writes provisional, JD-only values for `Role Summary`, `Location`, and `Priority` to every `New`-status role it processes (plus `JD Fetch Status`, now also always-overwrite, and `JD Body`, which stays write-only-to-empty/best-available). When a role is promoted to `Needs Research` and later reaches full intake, the career coach redoes these values from scratch using full research and **always overwrites** them, exactly like every other coach-owned property except the three named exceptions. The coach never treats Prioritization's values as a draft to confirm or correct. | `skills/career-engine-intake/SKILL.md` Step 0.9a (the always-overwrite rule and the three per-property bullets for `Role summary`/`Location`/`Priority`) | `agents/role-prioritizer.md` (writes the provisional values), `skills/role-prioritizer/SKILL.md` (states the overwrite semantics as the reason the pipeline exists), `skills/database/SKILL.md` → Property Ownership (documents the provisional/overwrite relationship) | If write-only-to-empty were left in place for these three fields, every role reaching full intake would already have Prioritization's cheap values populated, so the coach's research-informed values would never get written — the pipeline would be permanently stuck with the cheap guesses. Verified this does not create a false coach-complete: Step 0.8's coach-complete checklist requires 13–14 fields (`Priority Reason`, `Role emphasis`, `JD proof`, `Keywords`, `Strategy`, `Role Type`, `Relationship type`, `Gap handling`, `Culture`, `Landscape`, `First Advertised`, etc.), none of which Prioritization writes — a Prioritization-only role is always missing at least 8–9 required fields and can never be mistaken for coach-complete. |
| **Prioritization → full-intake queue selection.** Step 0.7's `scored`/`unscored` queue-selection buckets predate the Prioritization pipeline and originally assumed `Priority` being set meant a role had already been through a full (if writeback-failed) coach run. Prioritization breaks that assumption — it now writes a provisional `Priority` to nearly every role before promotion to `Needs Research`, so most roles reaching intake arrive already "scored" without ever having seen the coach. Step 0.7 now orders `scored` roles (regardless of whether the score came from Prioritization or a prior coach run) by Priority ascending first, filling any remaining slots with genuinely `unscored` roles (random) only as a fallback — this is what makes Prioritization's triage output actually drive full intake's 5-role selection, rather than the two pipelines coexisting without Step 0.7 ever reading Prioritization's signal. | `agents/role-prioritizer.md` (states this exact intent — "so the next full-intake run's 5-role selection has real signal instead of picking blindly among unscored roles" — as the reason Prioritization exists) | `skills/career-engine-intake/SKILL.md` Steps 0.6 (scored/unscored definition, now sourced from either pipeline) and 0.7 (the selection order itself) | Before this fix, Step 0.7's selection algorithm never actually used Prioritization's output: bucket 1 required `unscored` (empty Priority) and bucket 2 required `scored` **and** coach-complete — a Prioritization-only role (scored, not coach-complete) fit neither bucket definition and had no defined selection order, so the two pipelines shipped in the same session without the newer one actually informing the older one's queue selection, despite that being Prioritization's stated purpose. |
| **Each status's fast-path view URL must match that status's own saved filter — never borrow a sibling status's view.** A Notion view always returns exactly what its own saved filter shows (§2 Path B / §3 of the adapter); there is no way to override a view's saved filter at query time. Every `database_*_view_url` config key names the view for exactly one status: `database_new_view_url` → `New`, `database_hold_view_url` → `Needs Research`, `database_interested_view_url` → `Interested`, `database_researched_view_url` → `Researched`, `database_cv_ready_view_url` → `CV Ready for Review`, `database_edit_view_url` → `Needs Editing`. | `skills/database-notion/SKILL.md` §2/§3 (the view-always-returns-its-own-filter mechanic) | `agents/role-prioritizer.md` Step 0/Step 1 (resolves `database_new_view_url` for its `New` query and `database_hold_view_url` only for its explicit secondary `Needs Research` mode — never the same key for both), `skills/career-engine-setup/SKILL.md` (collects and writes all six view URLs), `references/pipeline-preferences.json` (documents all six keys) | **Confirmed production failure:** `role-prioritizer.md` resolved `database_hold_view_url` as its "fast-path to the queue view" and queried it looking for `New`-status roles. That view's saved filter is `Status = Needs Research`, which by definition excludes `New` roles — a live run against it returned zero results every time. Fixed by adding a dedicated `database_new_view_url` key and repointing role-prioritizer's preflight at it; the fix generalizes to any future status that gains its own fast-path key. |
| **Role-prioritizer cannot delegate the Path B view-query call — it must never reach Path B unassisted.** `database-notion/SKILL.md`'s "always delegate Path B to a subagent" rule (see the Notion read/write mechanics row above) assumes the caller runs in the main session, which has Task/Agent-tool access. `role-prioritizer` is itself spawned as a subagent with no such grant, so it structurally cannot follow that rule. | `agents/role-prioritizer.md` Step 1 (states the constraint and the stop-and-report fallback when Path A1/A2 both fail) | `skills/database-notion/SKILL.md` §2 (the delegation rule this agent is the one documented exception to) | **Confirmed production failure:** a live "run intake" session's role-prioritizer subagent fell through to Path B, received a single-line 425KB raw view-query payload directly into its own context, and burned roughly 50 tool calls trying to grep-parse generic field names ("Position", "Company", "id") out of it before giving up — grep matched the entire line as one hit every time. Fixed by having the agent prefer Path A1/A2 (bounded, filtered results) and stop-and-report instead of attempting Path B at all when both fail, rather than trying to hand-parse an oversized blob it has no way to keep out of its own context. |
| **`career-data` self-locate must search broadly, not stop at one or two fixed path guesses — and must actually request access on a permission-boundary hit (2026-07-10 addition), not just keep guessing.** The same "search under `~/.claude/skills/` and the Desktop app skill paths" phrasing appears in every standalone-entry agent's self-locate step; in a connected-folder sandbox (Cowork-style host-loop sessions), skills mount at a session-specific root that isn't `~`, so a hardcoded guess can fail even when `career-data` is present and reachable. The 2026-07-06 fix taught both files to recognize "outside connected folders" as a distinct signal from genuine not-found, but its only remedy was "keep searching the remaining candidates" — it never called the directory-access tool that error itself points at. | `agents/role-prioritizer.md` (self-locate, R-37) and `skills/career-engine-intake/SKILL.md` Step −0.5 (both now call `mcp__ccd_directory__request_directory` with the exact candidate path on an "outside connected folders" hit, before falling through to the next candidate; if the tool is unavailable or the user declines, behavior is unchanged from before this addition) | Any other standalone-entry agent with its own self-locate step, if one is added later — `agents/humanizer.md`/`skills/humanizer/SKILL.md` §R-37 and `agents/mind-dump.md` are confirmed NOT at parity even with the 2026-07-06 fix (no broadened multi-candidate search at all, let alone this request-directory step) — bringing them up requires porting both fixes, not just this one, and is a separate follow-up | **Confirmed production failure (2026-07-06 fix):** the same "run intake" session's first role-prioritizer subagent tried exactly `~/.claude/skills/career-data` and a bare `/Users/...` path, got "outside connected folders" on the second, and stopped — reporting career-data as not installed — while the main session located the same skill seconds later with a broader search. **Second, distinct confirmed production failure (this 2026-07-10 fix):** a later real Cowork session hit the *same* "outside connected folders" error on every career-data candidate path — the connector's own error message said to request access via a directory tool — and the agent, already carrying the 2026-07-06 broadened-search fix, still never called it; it exhausted every candidate and told the user to check Desktop app install settings, a wrong diagnosis for a one-click-fixable permission gap. Broadening *which paths* to guess (2026-07-06) and *acting on* a detected permission boundary (2026-07-10) are two different fixes; the first does not imply the second. |
| **The tool that resolves `${CAREER_DATA}` and the tool a spawned subagent reads it with are not guaranteed to be the same tool surface (2026-07-11 addition) — a third, distinct failure mode from the two rows above, neither of which is "career-data wasn't found."** Both prior fixes assume that once `career-data` is *located*, every consumer can reach it the same way. A real run broke that assumption: the orchestrator located `career-data` via a VM-backed tool, and the identical path was then unreachable via the orchestrator's own `Read` tool ("is a VM path... Read tool runs on the host filesystem") — the same error a `cv-writer` subagent hit independently moments later, with only `Read`/`Glob`/`Grep`/`Write`/`Edit` and no VM-capable tool of any kind. `cv-writer` correctly hard-stopped rather than fabricate, per its own doctrine — this row is not about that agent behaving wrong, it's about the orchestrator handing out a path with no verification that the tool surface receiving it could use it. | `skills/career-engine-orchestrator/orchestrator-queue.md` Step O1, `skills/career-engine-edit/SKILL.md` Step E0, `skills/career-engine-intake/SKILL.md` Step 0a.5 (each now runs a one-line `Read` reachability check on `career-data-marker.json` immediately after establishing its own run-scoped `$QUEUE_PIPE`/`$RUN_PIPE`/`$PIPE`; on failure, stages the small set of text files subagents actually read — never the `.dotx`/`.dotm` templates, which only this pipeline's own `pandoc` call touches — into a `career-data-staged/` subfolder of that same scratch directory, then reassigns `${CAREER_DATA}` to it for the rest of the run) | Every spawn instruction across all three pipelines, which all just interpolate `${CAREER_DATA}` and therefore need no change themselves — but if a future pipeline entry point is added with its own career-data resolution and no equivalent check, it inherits the same gap. Intake's Inline mode is a named, accepted gap (no `$PIPE` exists there to stage into) rather than silently uncovered — see the note at Step 0a.5. | **Confirmed production failure:** a real New Application pipeline run got through career-data discovery, the health check, and 28 roles' worth of Notion queue-building cleanly (proving the Notion connector and subagent spawning both worked normally in this session), then failed specifically at the first `cv-writer` spawn — 7 tool calls and ~25K tokens burned discovering, only after the fact, that the resolved `${CAREER_DATA}` path was categorically unreachable by any tool `cv-writer` had. The fix verifies reachability once, cheaply, immediately after career-data is confirmed healthy — before any subagent that depends on it is ever spawned — rather than discovering the gap per-subagent, per-role, deep into a real run. |
| **Intake must stop and report when a prerequisite pipeline hasn't run — never hand-roll a substitute for either pipeline's procedure.** Finding 0 `Needs Research` roles is a signal to suggest running Prioritization first, not license to improvise Prioritization's writes by hand and then improvise intake's own Steps 0.4–0.9e too. | `skills/career-engine-intake/SKILL.md` (the 0-roles branch, next to Step 0b's role-count report) | Every mandatory gate downstream of that decision: Step 0.4 (`$PIPE`), Step 0.8.5 (gatekeeper Coach Output Check), Step 0.9e (outreach map) | **Confirmed production failure:** the same "run intake" session found no `Needs Research` roles, so the orchestrating session hand-wrote Prioritization's five properties directly to Notion for 4 `New`-status roles (no `role-prioritizer` spawn), then built its own ad hoc `queue.md` and called the career coach directly — skipping `$PIPE` entirely, never running the Step 0.8.5 fabrication/field-fit gate before writing to Notion, and never running Step 0.9e's outreach-map write at all. This is the same "route around friction instead of stopping" shape as the June-18 `career-data` direct-write rationalization and the coach-output hand-edit incident, applied to two whole pipelines instead of one file. |
| **CV Type resolution — single resolution point per role, database consulted only under Variant.** `cv_type.mode` (`Detailed`/`Brief`/`Variant`) is read exactly once per role, early in the New Application and Edit pipelines (Step 0.type / Step E0.type) — the database backend's per-role `CV Type` field is read only when `mode` is `Variant`, never otherwise. The resolved value is written to `$PIPE/cv-type.txt`; every downstream spawn (`cv-writer`, `gatekeeper` option=cv, the export template-selection step) reads that file and never re-derives the value itself. | `skills/career-engine-new-application/SKILL.md` Step 0.type, `skills/career-engine-edit/SKILL.md` Step E0.type | `agents/cv-writer.md`, `agents/gatekeeper.md` (CV Check option), `skills/gatekeeper-checks/SKILL.md` (CV Check gates 0/2 branching), `skills/career-engine-export/SKILL.md` (`$CV_TEMPLATE` vs `$CV_TEMPLATE_BRIEF` selection) | A consumer that re-reads config or the database independently of the resolution step risks reading a different value mid-pipeline (e.g. if the user edits the Notion field between spawns) — the same class of drift the "single resolution point" pattern already prevents elsewhere (e.g. the coach's template-selection ownership row above). |
| **`$CV_FOOTER`/`$CV_FOOTER_HE` career-data resolution (2026-07-09 fix) — the CV footer joins the templates in never being plugin-hardcoded.** `convert-cv.sh` previously took a `PLUGIN_DIR` argument and hardcoded `${PLUGIN_DIR}/skills/career-engine-export/static-cv-footer.md` for every English CV export (the Hebrew pipeline hardcoded the equivalent `-he.md` path directly in three call sites). Fixed the same way as the `$CV_TEMPLATE`/`$CL_TEMPLATE` 2026-07-04 fix: `convert-cv.sh`'s arg 4 is now the resolved `$CV_FOOTER` path itself, and every caller resolves `$CV_FOOTER` = `${CAREER_DATA}/references/static-cv-footer.md` / `$CV_FOOTER_HE` = `${CAREER_DATA}/references/static-cv-footer-he.md` before calling it — never the plugin's own copy at runtime. | `skills/career-engine-export/scripts/convert-cv.sh` (the script itself) | `skills/career-engine-orchestrator/orchestrator-queue.md` and `skills/career-engine-edit/SKILL.md` (both Template resolution blocks), `skills/career-engine-export/SKILL.md` (Templates list, Step 3 call site, Hebrew production protocol), `skills/career-engine-new-application/SKILL.md` (Hebrew CV export step), `skills/career-engine-setup/SKILL.md` (Document templates — now also copies the blank footer files to career-data and asks the user to fill in her real Education/Languages at setup, since a blank `{{...}}` footer is an incomplete CV, not a decoration) | **Confirmed production bug, not theoretical — the reason this fix exists.** The plugin's own shipped `skills/career-engine-export/static-cv-footer.md` (and its Hebrew twin) had accumulated one real user's actual degree/university content (specific MA/BA programs and institutions) rather than staying a blank `{{...}}` template. Because the script hardcoded this plugin-side path, every installation of this plugin was silently appending that real person's Education/Languages section onto every user's exported CV, regardless of who installed it. Both plugin-side files are now scrubbed to `{{...}}` placeholders and only ever used as new-user setup defaults. |
| **CV footer injection is optional, `cv_footer.inject` (2026-07-12 fix) — the one part of Template resolution that isn't always-required.** Every other resolved path (`$CV_TEMPLATE`, `$CL_TEMPLATE`, `$CV_TEMPLATE_BRIEF`, the two Hebrew templates) has no on/off switch; `$CV_FOOTER`/`$CV_FOOTER_HE` now do. Read `cv_footer.inject` from `pipeline-preferences.json` (default `true`, so a config written before this feature is unaffected). `true`: resolve and require `$CV_FOOTER` exactly as the 2026-07-09 fix already did (row above). `false`: set `$CV_FOOTER`/`$CV_FOOTER_HE` to the empty string, never check either file's existence, never require them — `convert-cv.sh` and `assemble_brief_cv.py` both treat an empty path as "skip this content" rather than an error. This is for a user who already adds Education/Languages herself outside the pipeline (her own Word macro run after export) and doesn't want a second, plugin-managed copy to keep in sync — the same "not this pipeline's job" treatment the CV's optional `## ADDITIONAL` section has always had. | `references/pipeline-preferences.json` (`cv_footer.inject`, the new key) | `skills/career-engine-export/scripts/convert-cv.sh` (arg 4 now optional — empty string skips the footer-append instead of erroring), `skills/career-engine-export/scripts/assemble_brief_cv.py` (`--cv-footer` now optional — omitted `EDUCATION`/`LANGUAGES` sidebar entries when absent, mirroring the pre-existing `--additional`-omitted-when-absent pattern), `skills/career-engine-orchestrator/orchestrator-queue.md` and `skills/career-engine-edit/SKILL.md` (both Template resolution blocks, now split into an always-required paragraph and a separate conditional-on-`cv_footer.inject` paragraph), `skills/career-engine-export/SKILL.md` (Templates list), `skills/career-engine-setup/SKILL.md` (now asks before assuming the pipeline should manage this content, rather than always creating and prompting to fill in `static-cv-footer.md`) | A consumer that still treats `$CV_FOOTER` as unconditionally required (as the 2026-07-09 fix originally made it) will hard-stop a run for a user who deliberately opted out and has no `static-cv-footer.md` by design — not a missing-file bug, an unread config key. |
| **Self-declared "condensed process" is prohibited; mechanical-inline-fix excludes custom-style markup; the humanizer is non-skippable; mid-spawn CAREER_DATA loss has a sanctioned, gatekeeper-only orchestrator-verification fallback; `$PIPE` filesystem availability is now detected and disclosed (2026-07-12/13 addition).** A real Cowork run's own delivered feedback file admitted: *"This run used a condensed process throughout, in the interest of practical time constraints: gatekeeper mechanical fixes were frequently applied directly by the orchestrator rather than always re-spawning the writer agent, and a dedicated humanizer pass was only run where explicitly noted."* No such mode exists anywhere in this plugin's doctrine (confirmed absent repo-wide) — it was invented mid-run under perceived pressure from two real, confirmed Cowork capability gaps: no shared filesystem between the orchestrator and its subagents, and no resumable-subagent (SendMessage) capability. Five fixes, all doctrine-only (no code changes): (1) a 4th named anti-pattern block in Absolute Constraints prohibiting any self-declared degraded/condensed mode, tying the mechanical-inline-fix authorization and humanizer step to their exact documented scope regardless of time/cost pressure; (2) the mechanical-inline-fix authorization (the only place in the plugin that lets the orchestrator hand-edit CV text instead of re-spawning `cv-writer`) now explicitly excludes anything touching pandoc custom-style markup, plus a `custom-style=`-count self-check that reverts and re-spawns if an edit drops annotation markup; (3) the humanizer step is explicitly stated to have no time/cost exception, ever — only its existing 2-round-cap-then-halt-to-`halted-roles.json` path is sanctioned; (4) a gatekeeper subagent losing CAREER_DATA access mid-spawn (distinct from never having had it) now has a named, sanctioned fallback — the orchestrator verifies the claim itself against source files, since its own preflight already confirmed reachability — but this **never** extends to `cv-writer`/`letter-writer`, which must always hard-stop per existing R-37 doctrine; (5) a new capability check (`$PIPE_FILESYSTEM_AVAILABLE`, canary-file based) detects when no shared filesystem exists between the orchestrator and subagents, discloses it to the user in chat once (unlike the silent-log-once treatment of the pre-existing `$SENDMESSAGE_AVAILABLE` check, since this one changes content-fidelity risk, not just cost), and activates a byte-for-byte manual-relay rule (never retype/clean/summarize a prior subagent's returned text, including custom-style markup) for the rest of the run. | `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (the new anti-pattern block; the mid-spawn CAREER_DATA sub-rule inside the existing Step O1 staging-fallback section) | `skills/career-engine-new-application/SKILL.md` (Step 1.5's custom-style carve-out; the new Step 0.pipe.5 capability check; Step 5.9's non-skippable-humanizer pointer), `skills/career-engine-edit/SKILL.md` (the new Step E0.pipe.5; Step E8's non-skippable-humanizer pointer; confirmed to still have zero "mechanical and unambiguous" matches — this file never had inline-fix authorization and deliberately still doesn't, since always-re-spawn is strictly safer and adding one here would just be new attack surface for the exact failure this row closes) | **Confirmed production failures, one real Cowork run:** a cover letter (Cytactic) got measurably *worse* across 3 gatekeeper-fix rounds — new violations introduced, confirmed by the user directly — because the fix-log-and-re-spawn discipline was bypassed by hand-editing instead; the humanizer was silently skipped for the same role with zero doctrinal basis; custom-style annotation markup was silently stripped from a CV between a correctly-annotated draft and its very next (revision) step, traced to manual content relay in a no-shared-filesystem environment; and a gatekeeper subagent lost CAREER_DATA access mid-spawn on a different role (Global-e) despite the 2026-07-11 reachability fix already being live for that exact run — that fix covers only the orchestrator's own once-per-run preflight, not a subagent losing access to an already-resolved path partway through its own later spawn. |
| **`Role emphasis` conditionally carries a CV-type recommendation — not a new property.** When `cv_type.mode == "Variant"`, the career coach appends a one-line `Recommended CV Type: Detailed \| Brief — rationale` clause to the end of the `Role emphasis` text it already writes for full-research roles, sourced from a new CV Type Recommendation Matrix (Geography × Seniority × Tech-vertical). This is advisory only — it never writes to the user-owned per-role `CV Type` database field. When `mode` is `Detailed` or `Brief`, `Role emphasis` carries no such clause and looks exactly as it does today. | `skills/career-coach/coach-analysis.md` (the CV Type Recommendation Matrix, with its sourcing caveat for the Israel/South Africa/South America rows) | `skills/career-coach/coach-output.md` (the conditional clause documented inline in the `Role emphasis` template entry), `skills/career-engine-intake/SKILL.md` Step −1.5 (resolves `cv_type_mode` and passes it to the coach spawn — no new Notion write logic, since `Role emphasis` is written exactly as returned), `skills/database/SKILL.md` (Property Ownership note against the existing `Role emphasis` entry) | No new mandatory-field-parity entry was needed anywhere (unlike most new coach-owned fields) — because this isn't a new field, there's nothing for the three coach-field-parity lists to drift out of sync on. A consumer that assumes a separate `Recommended CV Type` property exists has misread this contract. |
| **QA is three layers, and the parity manifest must be updated in the same session as the lists it tracks (2026-07-13 restructure).** Layer 1 — `scripts/qa-mechanical.sh` (presence/count greps, extracted from qa-plugin.md's former inline bash blocks; also invoked standalone). Layer 2 — `scripts/qa-parity.py` + `scripts/qa-parity.json` (relational: list parity across sibling enumerations, defined-term closure, spawn-parameter context checks, `$PIPE` producer/consumer closure), run automatically at the end of Layer 1 AND by the pre-commit hook. Layer 3 — `agents/qa-plugin.md`'s judgment work: the diff-scoped consumer sweep (Phase 0-D) and the semantic passes (Checks 67-69: trace-a-run, contradiction hunt, enforcement red-team). **The load-bearing discipline: any edit that adds a member to a manifest-tracked list (coach mandatory fields, intake Step 0b capture), or names a new policy other files cite, must extend `qa-parity.json` in the same session** — the manifest is what converts a one-list edit into a forced sweep of every sibling list. | `scripts/qa-parity.json` (the manifest), `scripts/qa-parity.py` (the engine) | `scripts/qa-mechanical.sh` (invokes it), `scripts/check-invariants.sh` (pre-commit invokes it), `agents/qa-plugin.md` Phase 0-M/0-D (doctrine), the lists themselves in `skills/career-engine-intake/SKILL.md`, `skills/gatekeeper-checks/SKILL.md`, `skills/career-coach/coach-output.md` | A list gains a member without a manifest update → the parity layer can't force the sibling sweep and the pre-2026-07-13 drift pattern (three confirmed coach-field incidents, the 0b capture gaps, `cv-final.md`, `locked-fixes`, the halt-policy naming) resumes. A region anchor reworded without updating the manifest → that set silently stops checking (the engine FAILs loudly on a missing anchor, so this surfaces at the next commit). |
| **Cover-letter voice-ban batch + pipeline-input content boundary (2026-07-14 addition).** One real shipped letter batch, rejected outright by the user, surfaced: a family of agent-voice tells now banned at Gate 6 Tier 1 (`mandate`, figurative `system`, "from a blank page," "for exactly that"/"exactly that," "in their flow," "toward the" motion constructions, "handed me" employer-agency constructions, and the read-the-posting family — "I've read the [Role] posting twice," any version); a builder-origin phrase pile-up ("zero to" now mechanically capped at one occurrence per letter, zero when "from scratch" appears — three shipped in one letter); company-research recitation (org size/ownership/funding/founding-year facts recited back at the reader — the "100,000-person organization SAP folded WalkMe into" clause); a manufactured passion hierarchy (a genuine PLG-tagged Bank interest escalated into "cares about it more than anything in marketing," which she never said); and an unsourced prior-state employer claim ("I rebuilt what existed at [Company]" where the record shows she built from nothing). Root cause of the last three: pipeline inputs (Role summary, coach context block, coach outline, Landscape-derived facts) treated as content sources — now declared **targeting signals only**, in both the doctrine and the agent file. Also fixed at the source: `writer-craft` §8's worked opener example previously modeled "I read [Company]'s [Role Title] posting and..." — the shape the rejected batch escalated into the read-it-twice opener — and no longer models reading-narration at all (the same example-models-the-failure-mode pattern as the 2026-07-10 §8 fix). | `skills/writer-craft/SKILL.md` §2 (the ban table + "zero to" cap + read-the-posting ban), §3 (prior-state claims), §8 (reworked example), §10 (recitation ban, input boundary, passion hierarchy), §11 (new self-check items) | `skills/gatekeeper-checks/SKILL.md` Gate 4 (recitation + passion-hierarchy hard fails; prior-state added to the overreach item) and Gate 6 Tier 1 (new literal bans + builder-origin phrase cap — automatically inherited by both pipelines' orchestrator Bash backstops, which defer to Gate 6's Tier 1 section by name rather than enumerating words), `agents/letter-writer.md` (input-content boundary under Inputs from the orchestrator) | A new writer rule without its gate (or vice versa) reproduces the WI7 failure mode. A future edit that re-treats coach research as letter material — or reintroduces a reading-narration worked example into §8 — reproduces the exact rejected-batch failure this row closes. |
| **Gap-handling-disabled must reach the letter track; Gate 9's Objection-Preemption is satisfiable by demonstration; assemble-before-compose is a drafting ORDER; and a role has exactly two terminal states (2026-07-14 additions, same rejected batch).** Four related fixes: (1) **Never name the gap, letter-wide** — a real exported letter (Global-e, 2026-07-13) named the target domain "unfamiliar" twice in a manufactured "objection-preemption" passage, for a user whose `gap_handling` config is `disabled`; the coach/intake side already suppressed gaps when disabled, but nothing in the letter track did. Gate 9's Block-presence requirement ("names and answers the hesitation") was itself the pressure that manufactured the passage — reworded so answering purely by demonstration satisfies the block and deficit-naming FAILs it (under Gate 4's new deficit-naming rule). Writers must also consult the user's own guardrail mappings in career-data's `01-writing-rules.md` ("do not flag as a gap" rows with exact framing) before treating anything as a gap. (2) **Read-the-posting grep widened** — a second letter in the same batch shipped "is the one I read twice," which the original `read it twice`-style fragments missed; fragments now `read.*twice`/`twice.*read`. (3) **Assemble-before-you-compose** — verbatim assembly from career-data (Bank rows, delivered-letter phrasings, approved summaries, framework lines) is now the mandatory FIRST drafting move, with fresh composition only for gaps between assembled pieces; per the user directly, most of the batch's violations would not exist if writers copied her material and adjusted connectors instead of composing fresh and calibrating after. (4) **Two terminal states** — the Firefly AI role (2026-07-13) had a fully-passing letter whose revision log claimed "shipped to DOCX" while the files never reached the output folder ("binary transfer limitation... delivered via chat"), and the role then vanished from run-metrics entirely (neither processed nor halted); a new named anti-pattern in Absolute Constraints requires every Step O4 role to end verifiably completed clean or delivered flagged — BOTH with files on disk confirmed by listing (`halted-roles.json` is the flagged ledger; legacy filename), with DOCX-write failure = flagged delivery + staged markdown (the markdown always ships). | `skills/writer-craft/SKILL.md` §10 (never-name-the-gap + guardrail-table-first + disabled semantics) and §12 (drafting-order paragraph), `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (the third-terminal-state anti-pattern) | `skills/gatekeeper-checks/SKILL.md` Gate 4 (deficit-naming) + Gate 9 (Objection-Preemption redefinition) + Gate 6 (widened read-twice grep), `agents/letter-writer.md` (guardrail-table-first rule under Interview-trigger gaps; Assemble-before-you-compose step in Option 1 Write) | Re-narrowing Gate 9 to "names and answers" re-manufactures gap talk for disabled-gaps users; dropping the guardrail-table consult reverts writers to inventing gap framing the user has already written exact instructions against; a completion claim without a file listing lets exported-in-name-only roles vanish from the structural record again. |
| **Letter-writer input contract — exactly three content inputs, allowlist not blocklist (2026-07-14, per the user's direct instruction). ⚠ Amended 2026-07-16, per the user's direct instruction: `$PIPE/coach-outline.md` is BACK IN — the coach's bare paragraph-subject outline is the writer's structural spine (structure only, never a content source) and doubles as the coach's own Option 4 review rubric; this row's blocklist mention of it below is historical.** The letter-writer receives, in BOTH pipelines: the **final CV**, **`Role emphasis`**, and **`Why I Want This Role`** (content), plus company name + role title (identity), `Strategy` and the coach's template-selection token (single-value routing), `$PIPE/voice-calibration.md` (her own career-data material), and revision-round violation/review files about its own letter. It NEVER receives — and is prohibited from reading out of `$PIPE` — `Role summary`, `Landscape`, `Culture`, `Keywords`, `Gap handling`, `Relationship type`, `JD Body`/JD text, the recruiter review, `$PIPE/coach-outline.md`, `$PIPE/coach-output.md`, or `$PIPE/role-properties.md`. Notable reversals this made: edit's E7 previously said "do NOT pass `Role emphasis`" while passing `Keywords`/`Gap handling` (now exactly inverted); New Application previously passed `Role summary` as "the JD proxy for the letter-writer," the recruiter review as letter input, and the coach outline — all removed. The recruiter review's interview-trigger gaps are now user-facing interview-prep feedback only (feedback file); the coach outline is the coach's own Step 5.3/E7.4 review rubric; Gate 4's "JD good-fit section addressed" item is retired (the writer can't see a JD); the letter-writer's own file carries an ignore-and-report rule if a banned input shows up in its spawn context anyway. Career-data self-loading (Motivation Bank, delivered letters, guardrail table) is explicitly unrestricted — the contract restricts pipeline/database inputs, never her material. | `skills/career-engine-new-application/SKILL.md` Step 5 (the ⛔ input-contract block — canonical statement), `agents/letter-writer.md` Invocations (the agent-side contract + ignore-and-report rule) | `skills/career-engine-new-application/SKILL.md` (Step 5 spawn list, resume-fallback list, Step 0.data role-properties note, Step 4.99/5.3 outline wording, intro line), `skills/career-engine-edit/SKILL.md` (E7 spawn list, resume-fallback list, E6.9/E7.4 outline wording, E1 readiness note), `skills/writer-craft/SKILL.md` §10 (allowlist paragraph + interview-trigger retirement), `skills/gatekeeper-checks/SKILL.md` Gate 4 (retired good-fit item), `agents/recruiter-reviewer.md` (interview-trigger section is user-facing only) | Any future spawn-site edit that re-adds a banned input "for context" reproduces the recited-research failure batch this contract exists to close; a gatekeeper gate that checks the letter against content only `Role summary`/the JD carries will fail letters for ignoring input the writer never saw (the retired Gate 4 item is the template for how to handle those). Both readiness gates (O2, E1) already require `Role emphasis` — do not drop it from them, it is now writer-needed. |
| **`gap_handling_mode` is a universal spawn parameter — every agent, both pipelines, every round (2026-07-14, per the user's direct instruction).** `$GAP_HANDLING_MODE` (`enabled`/`disabled`) is resolved once per run from `pipeline-preferences.json` → `gap_handling` (`disabled` when the value is `disabled` or empty; absent key on an older config = `enabled`) and passed as `gap_handling_mode=$GAP_HANDLING_MODE` on EVERY subagent spawn in the New Application and Edit pipelines — paired with the `CAREER_DATA=${CAREER_DATA}` restatement at all 38 spawn sites, including re-spawns and crash-recovery fallbacks. This also formalizes a term both readiness gates already cited (`gap_handling_mode = disabled`) that no file had ever actually defined or derived. Consumption when `disabled`: the gatekeeper's Gate 0 gap-handling keyword exception never applies; the recruiter never produces a flag asking a writer to address/acknowledge a gap in a document (interview-trigger gaps stay, as user-facing prep); the coach's outline and Option 4 letter review carry zero gap directives; the coach-review spawns pass `Gap handling` property content only when `enabled`; the humanizer's input contract sanctions the token as no-action (the one config token allowed alongside its letter/CAREER_DATA/voice-calibration inputs — an amendment to the humanizer input boundary row above); the letter-writer receives it as a routing token within its input contract (property content still never reaches it) and never names a gap regardless of value. **Mechanically enforced:** `scripts/qa-parity.json` → `pipeline-spawn-gap-mode` (any pipeline paragraph restating `CAREER_DATA=${CAREER_DATA}` must also carry `gap_handling_mode`, min_sites 34) plus the token added to both gatekeeper context sets. | `skills/career-engine-orchestrator/orchestrator-queue.md` Config resolution (derivation), the Universal spawn parameter blockquotes in `skills/career-engine-new-application/SKILL.md` and `skills/career-engine-edit/SKILL.md` (the per-agent meaning) | Every spawn site in both pipeline files (38 paragraphs), `skills/gatekeeper-checks/SKILL.md` Gate 0 (exception gating + graceful not-passed fallback), `agents/recruiter-reviewer.md`, `agents/career-coach.md` Option 4a/4, `agents/letter-writer.md` (routing-token line), `skills/writer-craft/SKILL.md` §10 (allowlist), `skills/humanizer/SKILL.md` (input contract), `scripts/qa-parity.json` | A new spawn site that restates `CAREER_DATA` without the token fails the pre-commit parity check; a consumer that re-infers "disabled" from an empty `Gap handling` property instead of reading the token reverts to the inference that let the Global-e letter manufacture gap framing for a disabled-gaps config. |
| **Flag-and-deliver replaces halt-before-export (2026-07-14, per the user's direct and repeated instruction — she has said for months she never wants a halt).** Cap exhaustion no longer withholds output: the role's best current markdown AND DOCX always export to the output folder; what changes is visibility — Notion status doesn't advance, `halted-roles.json` (legacy filename, now the flagged-roles ledger, entries gain `delivered_files`) records it at the moment it happens, run-metrics' `roles_halted` (legacy key name) lists it, the Final Chat Delivery carries a per-role "⚠ delivered with unresolved violations" addendum, and the feedback file leads with a warning section. CV-track caps CONTINUE the pipeline with the best current CV (recruiter review and the letter track still run); letter-track caps skip remaining quality steps and route straight to export. The 2026-07-10 halt was an over-correction to a real incident where a "flag" lived only in a revision-log file nobody surfaced — the loud-flag machinery is the actual fix, and it stays. **Do not reinstate a halt under any rationale; producing nothing is the one outcome the user has explicitly rejected.** A real single-word case forced this: a role halted undelivered over one banned word ("playbook") at the Step 5.3 cap — the user got no md, no DOCX, nothing to review, fix, or learn from. | `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (the policy, cited by both pipelines by the exact name "flag-and-deliver policy"; also the two-terminal-states anti-pattern, now completed-clean / delivered-flagged) | Every cap clause in `skills/career-engine-new-application/SKILL.md` (Steps 1.5, 4.5, 5.2, 5.3, 5.95) and `skills/career-engine-edit/SKILL.md` (E3.5, E5.5, E7.3, E7.4, E7.7, E8.5), `skills/career-engine-orchestrator/orchestrator-post-run.md` (run-metrics schema + flagged-role addendum), `hooks/hooks.json` (both prompts; also fixed: user-pasted run output is not evidence of an active run — a confirmed false positive blocked a dev-session question), `agents/qa-plugin.md`, `scripts/qa-parity.json` (term renamed) | Reinstating any "do not export" outcome at a cap clause directly violates the user's standing instruction and reproduces the playbook incident; renaming the ledger file or metrics key without updating post-run's readers breaks the flag's only reporting path. |
| **Revision focus: minimal-edit ladder + touched-text gate + diff-bounded revision check (2026-07-14).** Late-stage revisions kept introducing NEW violations while fixing flagged ones — two roles in one real batch were flagged exactly this way, both times inside freshly composed replacement text nothing had asked for. Three-part fix, all aimed at shrinking how much new prose a revision writes and verifying the shrinkage mechanically: (1) **minimal-edit ladder** (writer-side) — resolve each violation at the smallest edit class that fixes it: delete → swap in place → restructure the one sentence → rewrite the paragraph, escalating only when the class below can't resolve it; composing a new sentence to remove a banned word is climbing too far. (2) **Touched-text gate** (writer-side) — before returning, run the §§1-4 ban check on ONLY the sentences added/reworded this round (plus locked fix-log items); untouched text is out of scope. (3) **The diff-bounded revision check** (orchestrator-side, the name both pipelines' loops cite) — after every revision round, mechanically `diff` the pre-round snapshot against the returned text; every changed hunk must map to an item in this round's brief; an unmapped hunk goes back to the resumed writer quoted, never hand-patched. | `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (the diff-bounded revision check, extending the existing numbered-snapshot rule), `agents/letter-writer.md` Option 1b and `agents/cv-writer.md` Option 2 (ladder + gate) | The numbered-snapshot rule (which this makes load-bearing rather than forensic-only), the locked-fixes instruction and mechanical locked-fix checklist (this complements them: they protect already-caught and currently-applicable rules; the ladder/gate/diff protect against brand-new violations in brand-new prose) | Removing the ladder reverts writers to full-sentence rewrites for one-word fixes — the exact regression surface; a snapshot saved but never diffed is the pre-2026-07-14 state where out-of-scope edits shipped undetected. |
| **Letter-quality restoration batch (2026-07-16, per the user's direct instruction after reporting letters "consistently terrible").** Six related fixes from one root-cause investigation: (1) **the delivered-letters archive read is restored in pipeline mode** for both the letter-writer (Voice Gate: calibration file + INDEX + 3 letters) and the humanizer (Voice Calibration Protocol: same sample) — the 2026-06-30 removal was never authorized (see the NO SILENT CAPABILITY REMOVAL section at the top of this file); the voice-calibration file SUPPLEMENTS the archive, never replaces it, and no spawn-site wording may describe it as making the archive read unnecessary. (2) **The Annotated Exemplar is restored** to `writer-craft` §8 (dropped without authorization in the 2026-07-02 consolidation), punctuation-reconciled with §1. (3) **Gate 2 gained an integration check (Tier 1)**: a telegraphic WIWTR fragment pasted raw as prose FAILs — the personal-content exemption protects her wording from vocabulary bans, it does not certify note-form shorthand as finished prose ("Doing good. Win win." shipped verbatim and survived 16 gatekeeper passes). (4) **Gate 9 gained a discourse-flow check (Tier 2 item 34** — count moved 33→34 everywhere): every paragraph must connect to its predecessor; the coach's outline is the structural spine (`writer-craft` §12 "One argument, the coach's outline as the spine" + "Assembly semantics" are the writer-side source — **corrected same day per the user: WIWTR is a scrap pile of notes and never sets order; the outline was restored as writer input, and `Role emphasis` is the content selector**). (5) **The sentence-rhythm floor was reframed to archive-calibrated variation** in all three sites (`writer-craft` §4, Gate 7 + Tier 2 item 18, humanizer Final Gate items 1-2): a numeric quota can flag, only content can fix — the old "add one short (2-5 word) sentence" direction provably manufactured fragments. (6) **Coach Option 4 must review within the writer's input contract**: it may use `Role summary`/`Keywords` to judge aim, never to direct the writer toward content it has no sanctioned source for; its strongest direction points at HER underused/convoluted material. | `CLAUDE.md` (NO SILENT CAPABILITY REMOVAL section), `skills/writer-craft/SKILL.md` §4/§8/§11/§12 | `agents/letter-writer.md` (Voice Gate, file table, Assemble step), `skills/humanizer/SKILL.md` + `agents/humanizer.md` (calibration protocol, Final Gate 1-2), `skills/gatekeeper-checks/SKILL.md` (Gate 2, Gate 7, Gate 9, Tier 2 items 18/34, counts), `agents/gatekeeper.md` (`[n] of 34`), `agents/career-coach.md` Option 4, both pipelines' Step 4.9/E6.8 + humanizer spawn sites, `agents/qa-plugin.md` Phase 0-D item 5 | Re-introducing any "calibration is complete — no archive read needed" wording reproduces the exact unauthorized removal that drove the collapse; re-adding a numeric rhythm floor re-manufactures fragments; a coach review that demands Role-summary content forces the writer to invent positioning (the recited-research family) — all three are the confirmed failure modes this batch closes. |
| **`JD Body` is a cache, not a canvas — and the coach produces ONLY sanctioned outputs (2026-07-16).** `JD Body`'s only legitimate content is the job posting's verbatim text, supplied by intake Step 0.5's fetch ladder; the coach passes fetched JD text through untouched or returns nothing for it. Generalized: no coach property is a container for an invented extra deliverable (quote anthologies, letter-material dossiers, prep documents) — extra-analysis suggestions go in ONE Patterns line, never produced. Intake discards a coach-returned `JD Body` that differs in kind from the fetched text and writes the fetched text instead, logging the violation. | `skills/career-coach/coach-output.md` ("JD Body is a cache, not a canvas" + the Firefly anti-pattern) | `skills/gatekeeper-checks/SKILL.md` Coach Output Check items 10-11, `skills/career-engine-intake/SKILL.md` Step 0.9a `JD Body` bullet (source-of-truth + discard rule) | **Confirmed production failure (Firefly VP Marketing, reported 2026-07-16):** the coach filled `JD Body` with a multi-section anthology of the user's own career-data quotes mapped to the JD's headings — unsanctioned work at real cost, personal material duplicated into the tracker, the actual JD never cached, and nothing anywhere checked the property's content type. |
| **WIWTR is never a run blocker, and a flag must describe the delivered text (2026-07-16, from one real Firefly VP Marketing run).** Two orchestrator-behavior fixes: (1) a WIWTR that is empty or still carries unanswered `[COACH PROMPTS` questions is a normal input state — pass it through, never stop to ask the user for answers mid-run, never frame the role as "ready except for" WIWTR; anti-fabrication is preserved by the Motivation Bank path, not by halting, and the system's design goal is career-data carrying more of every letter over time (the WIWTR→Bank promotion loop is the mechanism). The real run stopped at role start demanding answers; the Stop hook forced it onward and the Bank-only letter came out fine — proving the design. (2) Flag accuracy: every violation cited in a flagged delivery must be re-verified against the exact exported markdown at delivery time; the same run's flag claimed the letter "never named Snyk/Portshift/Alcide/XM Cyber" while the delivered letter named all four — a stale-round ghost the user couldn't act on. | `skills/career-engine-new-application/SKILL.md` Step 4.9→5 (the full WIWTR-never-a-blocker statement), `skills/career-engine-orchestrator/orchestrator-queue.md` Absolute Constraints (flag accuracy, inside the flag-and-deliver policy) | `skills/career-engine-edit/SKILL.md` E7 gate (pointer to the same rule), `skills/career-engine-orchestrator/orchestrator-post-run.md` (the addendum this feeds), `agents/letter-writer.md` Sufficiency Gate / Option A pre-check (the mechanism that makes proceeding safe) | An orchestrator that re-learns to stop for WIWTR answers reproduces the homework-assignment stop the hook had to override; a flag written from round memory instead of the shipped file reproduces the unactionable ghost-violation report. |
| **Prioritization is a user convenience, never an intake prerequisite (2026-07-14, per the user's direct instruction).** Intake's 0-`Needs Research`-roles branch previously said "stop and report — suggest running Prioritization first," which made Prioritization a de facto gate on intake. Now: an empty `Needs Research` queue falls back to the `New` queue directly (same delegated fetch discipline, `database_new_view_url` fast path / "New" view by name), running the full unchanged intake pipeline on those roles — Step 0.7's existing `unscored`→random fill is the designed selection for un-prioritized roles, and the coach does the real deep work during intake regardless. Only when BOTH queues are empty does intake stop. The anti-improvisation rule is unchanged and explicitly restated: whichever queue runs, every mandatory gate runs — the prohibition was always about hand-rolling the pipelines' writes, never about which status feeds them. | `skills/career-engine-intake/SKILL.md` (the 0-roles branch, Step 0b area) | `agents/role-prioritizer.md` (unchanged — its purpose statement already says it exists to inform selection, not to gate intake), `skills/career-engine-orchestrator/orchestrator-queue.md` (unaffected — the Interested queue is separate) | Re-adding "run Prioritization first" as a stop condition re-blocks a user who never runs Prioritization; dropping the anti-improvisation tail reopens the hand-rolled-pipeline incident that motivated the original wording. |

---

## Agent file content types

Use this as a checklist when writing or reviewing any agent file.

| Content type | Belongs in agent? | Notes |
|---|---|---|
| **Identity / expert framing** | ✅ Yes | 3–6 lines. What kind of expert. What the output achieves. What makes this agent distinct. |
| **Scope boundary** | ✅ Yes | What this agent explicitly does NOT handle. One line per boundary. |
| **Invocations** | ✅ Yes | How the agent is called: pipeline mode (what inputs arrive, from where) vs standalone. |
| **Procedural gates** | ✅ Yes | Non-negotiable prerequisite checks. Binary: pass or stop. |
| **File loading table** | ✅ Yes | Mandatory files before doing anything. One row per file; what it contains. |
| **Input list** | ✅ Yes (brief) | What each pipeline input IS and its routing function. Not how to use it — that's doctrine and goes in the skill. |
| **Options routing** | ✅ Yes | Named modes; one line each; pointer to section. |
| **Procedural steps** | ✅ Yes | Step-by-step: what to do, in what order. No explanatory doctrine — steps reference the skill for rules. |
| **Decision logic** | ✅ Yes | Ordered conditionals for revision or exception handling. Decision 1 → 2 → 3. |
| **Mandatory gates** | ✅ Yes | Non-negotiable final step before returning output. Never skippable. |
| **Output format spec** | ✅ Yes | Exact format of what to return. Per option if they differ. |
| **Integration rules** | ❌ No → Skill | How inputs interact; which source governs what. This is doctrine, not procedure. |
| **Craft rules / how to write** | ❌ No → Skill | What to write, how to write it, forbidden patterns. |
| **Templates** | ❌ No → Skill | Fill-in-the-blank patterns with labeled slots. |
| **Worked examples** | ❌ No → Skill | Before/after pairs, annotated exemplars. |
| **Personal info / candidate rules** | ❌ No → references/ | Any content specific to the candidate. |

---

## Skill file content types

Use this as a checklist when writing or reviewing any skill file.

| Content type | Belongs in skill? | Notes |
|---|---|---|
| **Core knowledge / philosophy** | ✅ Yes | Why the task matters; the mental model to hold. Sets the frame before any rules. |
| **What it must do** | ✅ Yes | Positive obligations with a test for each. |
| **What it must not do** | ✅ Yes | Prohibitions and named anti-patterns. Each with a correction or alternative. |
| **Integration rules** | ✅ Yes | How inputs from outside interact. Which source governs which decision. |
| **Input execution rules** | ✅ Yes | How to use specific inputs during composition (opener source, strategy analysis ban, etc.). |
| **Quantitative thresholds** | ✅ Yes | Exact numbers: word counts, keyword percentages, occurrence limits. |
| **Templates** | ✅ Yes | Fill-in-the-blank patterns with labeled slots. Named and numbered. |
| **Worked examples** | ✅ Yes | Concrete weak → strong pairs or annotated exemplars from real source material. |
| **Rules list** | ✅ Yes | Named, testable prohibitions. State the rule; state the correction. |
| **Mandatory checklists** | ✅ Yes | Ordered steps to run after drafting. Each step has a pass/fail criterion. |
| **Vocabulary — banned** | ✅ Yes | Words and phrases to cut outright, with explanation. |
| **Vocabulary — approved** | ✅ Yes | Alternatives organized by category or situation. |
| **Voice permissions** | ✅ Yes | Conditional allowances: things permitted but not required. |
| **Procedural steps** | ❌ No → Agent | What to do in what order belongs in the agent as Option steps. |
| **File loading instructions** | ❌ No → Agent | What to read before starting belongs in the agent's file table. |
| **Output format** | ❌ No → Agent | What to return and in what format belongs in the agent. |

---

## Key design decisions

**Why doctrine lives in skills, not agents:**
Agent files are open-source. Personal writing philosophy, specific candidate rules, and strategic framing would leak into the public repo if kept in agents. Skills hold doctrine and blank `{{...}}` templates only; the user's personal content lives entirely in the external `career-data` skill, never in the plugin.

**Why the pipeline cap is 5 roles:**
The career coach runs as a single subagent with all roles in context. Beyond 5, context quality degrades. The intake skill processes all roles when there are ≤5 (no priority ordering needed); priority ordering only applies when there are >5 and a selection must be made.

**Why view discovery replaces hardcoded view URLs:**
Notion view URLs change when views are reorganised. The view-discovery mechanism lives once in the Notion adapter (`skills/database-notion/SKILL.md` §3); every Path B query site — intake, the orchestrator, coach, edit, role-prioritizer, and source-open-roles — delegates to it. It fetches the database, reads the `<views>` list from that single response, and finds the view **by name** ("New" / "Needs Research" / "Interested" / "Needs Editing" / a broad view) rather than relying on a hardcoded URL that silently breaks. Any of the six optional config keys (`database_new_view_url`, `database_edit_view_url`, `database_interested_view_url`, `database_hold_view_url`, `database_researched_view_url`, `database_cv_ready_view_url`) can be pre-filled in `pipeline-preferences.json` to use as a fast path — skipping the 59KB DB discovery fetch — but the by-name lookup is always the fallback when a URL is empty or stale.

**The canonical read ladder — the Notion adapter's implementation (consistent across all query sites; see the *Database backend* glossary entry):** A1 (`ntn` CLI, gated, server-side filter) → A2 (`notionApi` `API-query-data-source`, structured filter) → B (standard connector `notion-query-database-view`). Path B is the connector-only fallback and has two hard constraints (R-39): `notion-query-database-view` accepts **no ad-hoc `filter` argument** (it runs the view's own saved filter) and requires a real **view URL** (`...?v=<id>`), never the bare database URL — so Path B is always *fetch-DB → find view by name → query that view URL (no filter) → discovery-only → per-page `notion-fetch` → discard non-matching Status*. If every rung fails, **stop and report** — never treat it as zero results, and never improvise `notion-search`/semantic search to discover queue rows. **The full ladder, view discovery, and writeback mechanics with syntax live in one place — the Notion adapter skill `skills/database-notion/SKILL.md`, loaded whenever `database_backend` is `notion` (the default).** Intake, the orchestrator, edit, and source-open-roles speak in generic operations (query the queue, fetch a record, write a field, resolve a view) and **delegate to the adapter** rather than re-describing it — so a mechanics fix is one edit, not many. (The career coach does no Notion ops — it returns its analysis and intake Step 0.9a writes it.)

**Why the opener rule is a principle, not a template:**
Delivered letters show openers that vary widely in structure — emotional reaction, existing relationship, personal tension, value claim, warm connection. The common thread is specificity: the reader knows that role the user is applying for in the first sentence; within the first two sentences the reader knows why this person wants this role (sum total: why this person is writing now to this company). A fixed template produces generic letters. The rule is: establish that context; how is up to the user's own words — the Why I Want This Role content when present, otherwise the role-matched Motivation Bank entries.

**Why Brief CV's `Earlier:` cutoff is a judgment principle, not a plugin config number:**
An early design draft for the Brief CV Type (the one-page, two-column condensed CV) tried to fix "how many roles get a full entry before collapsing into an `Earlier:` line" as a numeric rule in shared plugin doctrine — e.g. "the most recent 3-4 roles." This was rejected during design: the right number depends on a specific user's total career length, number of employers, and how much room her particular `.dotx` template actually has — none of which the shared plugin can know in advance, and none of which generalizes from one user to the next. The same reasoning as the opener rule above applies: `cv-writer` gets a judgment principle (fit the whole CV to one page; give the most recent/relevant roles full treatment; fold the rest into `Earlier:`) plus the one real, checkable fact it needs to apply it (`cv_type.brief_has_photo`, optional and per-user — whether the template has a photo, not a guessed capacity estimate), not a fixed number in `pipeline-preferences.json`. See `skills/writer-craft/SKILL.md` §5b and `agents/cv-writer.md`'s Brief-Specific Rules.

---

## Glossary

Shared terminology used throughout the plugin. When a term appears in an agent, skill, or this file, it means the thing defined here — not a colloquial sense.

| Term | Definition |
|---|---|
| **Pipeline** | A complete end-to-end workflow with a named trigger, entry skill, and owned Status transitions. Defined in the Pipeline Registry in `skills/career-engine/SKILL.md`. Examples: New Application, Edit, Intake, Prioritization. |
| **Prioritization** | The cheap, uncapped triage pipeline that runs before full intake. Reads `New`-status roles by default (optionally `Needs Research` roles on explicit request, to refresh a stale Priority). Per role: fetches the JD (reusing intake's Step 0.5 ladder), reads a plain JD-stated `Location` (not the deep multi-source scan `source-open-roles` does), scores `Priority` using the same Priority Framework the career coach uses (JD-only — no company/culture/landscape research), and writes a `Role Summary`. Writes `Role Summary`, `Location`, `Priority`, `JD Fetch Status`, `JD Body`, then promotes Status to `Needs Research` — the same status full intake already reads. Entry point: `agents/role-prioritizer.md`, deliberately thin and not a strategist persona, pinned to `model: haiku`. Its five written values are provisional; the career coach always overwrites `Role Summary`/`Location`/`Priority` when the role reaches full intake (see the cross-file-contract row). |
| **Agent** | A spawnable subagent that performs one discrete task (write a CV, run a gatekeeper check, review as a recruiter). Lives in `agents/`. Spawned by the orchestrator or entry skill — never auto-activated. |
| **Skill** | A doctrine or craft file loaded explicitly by an agent via `Read`. Not a pipeline step — it holds the rules, templates, and philosophy the agent applies. Lives in `skills/`. |
| **Check** (gatekeeper) | One of the three document-type check sets the gatekeeper can run: **CV Check** (`option=cv`), **Cover Letter Check** (`option=cover-letter`), **Coach Output Check** (`option=coach-output`). Not optional — which check runs depends on what the pipeline is validating at that step. |
| **Option** (agent invocation) | A named invocation mode for an agent that has more than one way to run. Example: `letter-writer option=draft` vs `option=revision` vs `option=standalone`. Options are agent-specific; they are not the same concept as gatekeeper checks. |
| **Hard fail** | A Tier 1 gatekeeper violation that always blocks the pipeline and returns the document to the writer, regardless of the Tier 2 score, on every round. |
| **Tier 2 check type** | One of the 34 named, binary check types (Gates 5-9, excluding Gate 5's Pattern A-J/Non-transferability-forcing-function items, Gate 6's curated banned-vocabulary/fit-declaration lists, and Gate 9's Block-presence/identity-idiom items, all of which are Tier 1; item 34 — discourse flow — added 2026-07-16) that make up the Cover Letter Check's Tier 2 score. Binary per type across the whole letter: 0 violations of that type anywhere = the type passes; 1+ violations anywhere = the type fails, regardless of instance count. Score = (types passed ÷ total applicable) × 100; **PASS ≥70%**. See **Tier 1 / Tier 2** below and the authoritative checklist and grading section in `skills/gatekeeper-checks/SKILL.md`. |
| **Tier 1 / Tier 2** | The Cover Letter Check's two-part grading model (replaces the retired Grade A-D table). **Tier 1** — Gates 1-5, Gate 6's curated banned-vocabulary/fit-declaration lists (added 2026-07-05), plus Gate 9's Block-presence and identity-idiom checks — is 100% required, no exceptions, and blocks every round; none of it is style, taste, or judgment-dependent. **Tier 2** — the 34-item checklist of named check types (see **Tier 2 check type** above) drawn from Gate 5's pattern-derivation forcing function (added 2026-07-08), Gate 6's one remaining judgment-dependent item (idiom/metaphor recognition), Gates 7-9 (Gate 9 here means only its Dial Sheet ceiling checks and the 2026-07-16 discourse-flow check), Gate 4's two harder-to-detect advisory items, and the generic-default-template-reuse check — requires an aggregate ≥70% and is **round-aware**: a Tier-1-clean letter with Tier 2 <70% FAILs back to the writer on round 1, but on round 2+ a Tier-1-clean letter is treated as PASS regardless of its Tier 2 score, with the remaining failing check types logged for the humanizer instead of looping further. Not to be confused with the unrelated **Tier** (keywords) entry below. |
| **Tier** (keywords) | Classification of JD keywords into Critical / Important / Nice-to-have for the gatekeeper's ATS pre-check. Thresholds: Critical ≥80%, Important ≥60%, Nice-to-have = advisory only. Unrelated to the Cover Letter Check's **Tier 1 / Tier 2** grading model above — same word, different mechanism. |
| **R-41** | Output protocol requiring pipeline subagents to write their full output to a `$PIPE/` file and return only a 1-line status. Keeps orchestrator context small. Violations bloat the run. Applies to inputs too, not just outputs: large content (JD text, row payloads, a subagent's prior output) passed between pipeline steps belongs in a `$PIPE/` file referenced by path, never pasted inline into a spawn prompt or re-read in repeated chunks. A `$PIPE/` file's content belongs to the agent that wrote it — another agent (including the orchestrator itself) reads it, but never hand-edits it; a revision goes back through the owning agent, always. |
| **R-37** | Data root rule: personal-data files load from `${CAREER_DATA}/references/`. Plugin files load from `${CLAUDE_PLUGIN_ROOT}/`. Never hardcode paths. |
| **Database backend** | The tracker that holds the user's job-application records. The pipeline speaks in generic operations — query the queue, fetch a record, write a field, resolve the edit view — implemented by **the Notion adapter** (the only backend shipped today). Its mechanics — the A1→A2→B read ladder, view discovery, and property/page-body writeback — are spelled out once in the **`database-notion` skill** (`skills/database-notion/SKILL.md`), a MANDATORY load whenever `database_backend` is `notion`; pipeline skills delegate to it. A future backend is a sibling adapter skill (e.g. `database-airtable`) with the same generic operations. Config names the backend via `database_backend` (default `notion`) + `database_id` + six optional view URL keys (`database_new_view_url`, `database_edit_view_url`, `database_interested_view_url`, `database_hold_view_url`, `database_researched_view_url`, `database_cv_ready_view_url`); the user-facing config is backend-neutral. Notion is an implementation, not an assumption. Internal `$NOTION_*` shell vars are the adapter's names and stay as-is. |
| **WIWTR** | Why I Want This Role — the user's first-person notes about a *specific* role, stored in the Notion job row. **Role-specific motivation, supplementary to the Motivation Bank** (the standing primary source). It is the primary role-specific input *when present*, but it is **not mandatory**: when WIWTR is empty, the letter-writer writes from the role-matched Motivation Bank entries if a genuine opener is possible, otherwise skips the role (Sufficiency Gate). When WIWTR is present, its distinct points must still all appear in the letter. |
| **Motivation Bank** | The `\| Tags \| Motivation \|` verbatim table in `background/background-motivation-bank.md` (reached via the router in `02-professional-background.md`). The letter-writer's **mandatory primary content/voice source** — loaded and used ahead of any constructed alternative. Each Motivation cell is the user's own words, kept word-for-word; the table is append-only. The WIWTR promotion step (new-application Step 7f; edit Step E10.5) appends new tagged verbatim rows; there is no separate "Promoted from Why I Want This Role" section. |
| **Surgical-only revision** | Letter-writer rule: reviewer feedback authorises touching only what was flagged. Every sentence not explicitly called out stays word-for-word. |
| **Fabrication** | Inventing credentials, outcomes, or experience not present in the user's documented background or WIWTR. Always prohibited. Reviewer flags never authorise fabrication. |
| **CV Type** | Which of two CV shapes `cv-writer` produces: **Detailed** (the original full CV) or **Brief** (a one-page, two-column condensed CV). Controlled by `pipeline-preferences.json` → `cv_type.mode`: `Detailed` or `Brief` apply to every role; `Variant` defers to the per-role, user-owned `CV Type` field on the configured database backend (fixed field name, not configurable like `location_compatibility` — this property has no pre-existing name to reconcile with), defaulting to `Detailed` when that field is empty for a role. Resolved once per role, early in the New Application/Edit pipelines (see the CV Type resolution cross-file-contract row above) — never re-derived at each downstream spawn. Under Variant mode, the career coach appends an advisory recommendation to `Role emphasis` during intake (see the `Role emphasis` cross-file-contract row above and `skills/career-coach/coach-analysis.md`'s CV Type Recommendation Matrix) — this never overwrites the user's own field. **Known limitation (resolved 2026-07-10):** the two-column visual layout cannot be produced by plain pandoc markdown (confirmed by testing, not just theorized — see `skills/career-engine-export/SKILL.md`'s Brief annotation reference); a post-processing script (`skills/career-engine-export/scripts/assemble_brief_cv.py`) now assembles it directly via python-docx, opening the actual `cv-brief.dotx` template and filling its cells by style name. **New scope limit this introduces:** the script hard-codes the default template's exact table shape and style names, so a personalized `cv-brief.dotx` is only supported when it's a cosmetic derivative (fonts/colors/spacing/alignment changed) — a template whose table structure was itself changed (rows/columns added or removed, merges changed, styles renamed) is rejected at export time with a specific error, not silently misrendered (`career-engine-setup/SKILL.md`'s Brief own-template step warns the user of this before she supplies a file). |

---

## Changelog rules

The changelog lives in `README.md` under `## Changelog`. Rules that must never be violated:

1. **Newest at the top.** Each new entry goes above all prior entries — never below them.
2. **Previous entries are never removed.** Existing entries must remain intact exactly as written. Condensing, rewording, or deleting old entries is prohibited.
3. **Entry heading format:** `### YYYY-MM-DD — <label>` where `YYYY-MM-DD` is the release date and `<label>` describes the shipment — e.g. `Bug fixes`, `Performance improvements`, `New features`, `Consolidated overhaul`. Use whatever label fits the contents; it is not fixed to the word "updates."
4. **One entry per release.** All changes in a single shipped `.plugin` belong under one date heading.

When documenting a release, use this structure (sub-headings within an entry are encouraged when the release is large):

```markdown
### 2026-MM-DD — <label>

**New features**
- **Feature name** — one-line description.

**Improvements**
- **Improvement name** — one-line description.

**Bug fixes**
- **Fix name** — what was broken and how it was fixed.
```

---

## QA Agent

See the mandatory stop gate at the top of this file. The QA agent lives at `agents/qa-plugin.md`. Run it after every edit session. It validates the single shipped `.plugin` artifact (unzipped) for stale references, missing files, property-name consistency, structural integrity, and that it contains zero personal data. Since 2026-07-13 it is three-layered: it first runs the mechanical battery (`scripts/qa-mechanical.sh`, which chains into the relational parity layer `scripts/qa-parity.py`), then the diff-scoped consumer sweep, then the manual/judgment checks and semantic passes (trace-a-run, contradiction hunt, enforcement red-team). The full check list is in the agent file itself; the parity contracts live in `scripts/qa-parity.json` (see the cross-file-contracts row above for the manifest-update discipline).